<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/month.php');
	include_once(__DIR__ . '/month_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('month');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'month';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`month`.`month_id`" => "month_id",
		"`month`.`month`" => "month",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`month`.`month_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`month`.`month_id`" => "month_id",
		"`month`.`month`" => "month",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`month`.`month_id`" => "Month ID:",
		"`month`.`month`" => "Month Model:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`month`.`month_id`" => "month_id",
		"`month`.`month`" => "month",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`month` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'month_view.php';
	$x->RedirectAfterInsert = 'month_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Month:';
	$x->TableIcon = 'resources/table_icons/3D-Calendar-red1.png';
	$x->PrimaryKey = '`month`.`month_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Month ID:', 'Month Model:', ];
	$x->ColFieldName = ['month_id', 'month', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/month_templateTV.html';
	$x->SelectedTemplate = 'templates/month_templateTVS.html';
	$x->TemplateDV = 'templates/month_templateDV.html';
	$x->TemplateDVP = 'templates/month_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: month_init
	$render = true;
	if(function_exists('month_init')) {
		$args = [];
		$render = month_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: month_header
	$headerCode = '';
	if(function_exists('month_header')) {
		$args = [];
		$headerCode = month_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: month_footer
	$footerCode = '';
	if(function_exists('month_footer')) {
		$args = [];
		$footerCode = month_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
