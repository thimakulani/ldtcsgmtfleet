<?php

// Data functions (insert, update, delete, form) for table movement_of_personnel_in_government_garage_and_workshops

// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

function movement_of_personnel_in_government_garage_and_workshops_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('movement_of_personnel_in_government_garage_and_workshops');
	if(!$arrPerm['insert']) return false;

	$data = [
		'vehicle_inspection' => Request::lookup('vehicle_inspection', ''),
		'vehicle_model' => Request::val('vehicle_model', ''),
		'vehicle_number_plate' => Request::val('vehicle_number_plate', ''),
		'vehicle_tires_check' => Request::val('vehicle_tires_check', ''),
		'vehicle_mirrow_check' => Request::val('vehicle_mirrow_check', ''),
		'gate_security_signature' => Request::fileUpload('gate_security_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'gate_security_signature', Request::val('SelectedID'));
			},
		]),
		'government_garage_protocol' => Request::val('government_garage_protocol', ''),
		'government_garage_safety' => Request::val('government_garage_safety', ''),
		'vehicle_handing_over_checklist' => Request::fileUpload('vehicle_handing_over_checklist', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'vehicle_handing_over_checklist', Request::val('SelectedID'));
			},
		]),
		'vehicle_return_list' => Request::fileUpload('vehicle_return_list', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'vehicle_return_list', Request::val('SelectedID'));
			},
		]),
		'approved_workshop_procedure_manual' => Request::fileUpload('approved_workshop_procedure_manual', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'approved_workshop_procedure_manual', Request::val('SelectedID'));
			},
		]),
		'vehicle_registration_number' => Request::val('vehicle_registration_number', ''),
		'engine_number' => Request::val('engine_number', ''),
		'make_of_vehicle' => Request::lookup('make_of_vehicle', ''),
	];


	// hook: movement_of_personnel_in_government_garage_and_workshops_before_insert
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_before_insert')) {
		$args = [];
		if(!movement_of_personnel_in_government_garage_and_workshops_before_insert($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$error = '';
	// set empty fields to NULL
	$data = array_map(function($v) { return ($v === '' ? NULL : $v); }, $data);
	insert('movement_of_personnel_in_government_garage_and_workshops', backtick_keys_once($data), $error);
	if($error) {
		$error_message = $error;
		return false;
	}

	$recID = db_insert_id(db_link());

	update_calc_fields('movement_of_personnel_in_government_garage_and_workshops', $recID, calculated_fields()['movement_of_personnel_in_government_garage_and_workshops']);

	// hook: movement_of_personnel_in_government_garage_and_workshops_after_insert
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_after_insert')) {
		$res = sql("SELECT * FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `movement_id`='" . makeSafe($recID, false) . "' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) {
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = makeSafe($recID, false);
		$args = [];
		if(!movement_of_personnel_in_government_garage_and_workshops_after_insert($data, getMemberInfo(), $args)) { return $recID; }
	}

	// mm: save ownership data
	set_record_owner('movement_of_personnel_in_government_garage_and_workshops', $recID, getLoggedMemberID());

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID'))) movement_of_personnel_in_government_garage_and_workshops_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function movement_of_personnel_in_government_garage_and_workshops_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);

	// launch requests, asynchronously
	curl_batch($requests);
}

function movement_of_personnel_in_government_garage_and_workshops_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('movement_of_personnel_in_government_garage_and_workshops', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: movement_of_personnel_in_government_garage_and_workshops_before_delete
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_before_delete')) {
		$args = [];
		if(!movement_of_personnel_in_government_garage_and_workshops_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: '' 
			);
	}

	// child table: movement_of_personnel_in_government_garage_and_workshops
	$res = sql("SELECT `movement_id` FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `movement_id`='{$selected_id}'", $eo);
	$movement_id = db_fetch_row($res);
	$rires = sql("SELECT COUNT(1) FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `vehicle_inspection`='" . makeSafe($movement_id[0]) . "'", $eo);
	$rirow = db_fetch_row($rires);
	if($rirow[0] && !$AllowDeleteOfParents && !$skipChecks) {
		$RetMsg = $Translation["couldn't delete"];
		$RetMsg = str_replace('<RelatedRecords>', $rirow[0], $RetMsg);
		$RetMsg = str_replace('<TableName>', 'movement_of_personnel_in_government_garage_and_workshops', $RetMsg);
		return $RetMsg;
	} elseif($rirow[0] && $AllowDeleteOfParents && !$skipChecks) {
		$RetMsg = $Translation['confirm delete'];
		$RetMsg = str_replace('<RelatedRecords>', $rirow[0], $RetMsg);
		$RetMsg = str_replace('<TableName>', 'movement_of_personnel_in_government_garage_and_workshops', $RetMsg);
		$RetMsg = str_replace('<Delete>', '<input type="button" class="btn btn-danger" value="' . html_attr($Translation['yes']) . '" onClick="window.location = \'movement_of_personnel_in_government_garage_and_workshops_view.php?SelectedID=' . urlencode($selected_id) . '&delete_x=1&confirmed=1&csrf_token=' . urlencode(csrf_token(false, true)) . '\';">', $RetMsg);
		$RetMsg = str_replace('<Cancel>', '<input type="button" class="btn btn-success" value="' . html_attr($Translation[ 'no']) . '" onClick="window.location = \'movement_of_personnel_in_government_garage_and_workshops_view.php?SelectedID=' . urlencode($selected_id) . '\';">', $RetMsg);
		return $RetMsg;
	}

	sql("DELETE FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `movement_id`='{$selected_id}'", $eo);

	// hook: movement_of_personnel_in_government_garage_and_workshops_after_delete
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_after_delete')) {
		$args = [];
		movement_of_personnel_in_government_garage_and_workshops_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='movement_of_personnel_in_government_garage_and_workshops' AND `pkValue`='{$selected_id}'", $eo);
}

function movement_of_personnel_in_government_garage_and_workshops_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('movement_of_personnel_in_government_garage_and_workshops', $selected_id, 'edit')) return false;

	$data = [
		'vehicle_inspection' => Request::lookup('vehicle_inspection', ''),
		'vehicle_model' => Request::val('vehicle_model', ''),
		'vehicle_number_plate' => Request::val('vehicle_number_plate', ''),
		'vehicle_tires_check' => Request::val('vehicle_tires_check', ''),
		'vehicle_mirrow_check' => Request::val('vehicle_mirrow_check', ''),
		'gate_security_signature' => Request::fileUpload('gate_security_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'gate_security_signature', $selected_id);
			},
		]),
		'government_garage_protocol' => Request::val('government_garage_protocol', ''),
		'government_garage_safety' => Request::val('government_garage_safety', ''),
		'vehicle_handing_over_checklist' => Request::fileUpload('vehicle_handing_over_checklist', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'vehicle_handing_over_checklist', $selected_id);
			},
		]),
		'vehicle_return_list' => Request::fileUpload('vehicle_return_list', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'vehicle_return_list', $selected_id);
			},
		]),
		'approved_workshop_procedure_manual' => Request::fileUpload('approved_workshop_procedure_manual', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('movement_of_personnel_in_government_garage_and_workshops', 'approved_workshop_procedure_manual', $selected_id);
			},
		]),
		'vehicle_registration_number' => Request::val('vehicle_registration_number', ''),
		'engine_number' => Request::val('engine_number', ''),
		'make_of_vehicle' => Request::lookup('make_of_vehicle', ''),
	];

	// get existing values
	$old_data = getRecord('movement_of_personnel_in_government_garage_and_workshops', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: movement_of_personnel_in_government_garage_and_workshops_before_update
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_before_update')) {
		$args = ['old_data' => $old_data];
		if(!movement_of_personnel_in_government_garage_and_workshops_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'movement_of_personnel_in_government_garage_and_workshops', 
		backtick_keys_once($set), 
		['`movement_id`' => $selected_id], 
		$error_message
	)) {
		echo $error_message;
		echo '<a href="movement_of_personnel_in_government_garage_and_workshops_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	$eo = ['silentErrors' => true];

	update_calc_fields('movement_of_personnel_in_government_garage_and_workshops', $data['selectedID'], calculated_fields()['movement_of_personnel_in_government_garage_and_workshops']);

	// hook: movement_of_personnel_in_government_garage_and_workshops_after_update
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_after_update')) {
		$res = sql("SELECT * FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `movement_id`='{$data['selectedID']}' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['movement_id'];
		$args = ['old_data' => $old_data];
		if(!movement_of_personnel_in_government_garage_and_workshops_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update ownership data
	sql("UPDATE `membership_userrecords` SET `dateUpdated`='" . time() . "' WHERE `tableName`='movement_of_personnel_in_government_garage_and_workshops' AND `pkValue`='" . makeSafe($selected_id) . "'", $eo);
}

function movement_of_personnel_in_government_garage_and_workshops_form($selected_id = '', $AllowUpdate = 1, $AllowInsert = 1, $AllowDelete = 1, $separateDV = 0, $TemplateDV = '', $TemplateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selected_id. If $selected_id
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = null;
	$row = $urow = $jsReadOnly = $jsEditable = $lookups = null;

	$noSaveAsCopy = true;

	// mm: get table permissions
	$arrPerm = getTablePermissions('movement_of_personnel_in_government_garage_and_workshops');
	if(!$arrPerm['insert'] && $selected_id == '')
		// no insert permission and no record selected
		// so show access denied error unless TVDV
		return $separateDV ? $Translation['tableAccessDenied'] : '';
	$AllowInsert = ($arrPerm['insert'] ? true : false);
	// print preview?
	$dvprint = false;
	if(strlen($selected_id) && Request::val('dvprint_x') != '') {
		$dvprint = true;
	}

	$filterer_vehicle_inspection = Request::val('filterer_vehicle_inspection');
	$filterer_make_of_vehicle = Request::val('filterer_make_of_vehicle');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: vehicle_inspection
	$combo_vehicle_inspection = new DataCombo;
	// combobox: make_of_vehicle
	$combo_make_of_vehicle = new DataCombo;

	if($selected_id) {
		// mm: check member permissions
		if(!$arrPerm['view']) return $Translation['tableAccessDenied'];

		// mm: who is the owner?
		$ownerGroupID = sqlValue("SELECT `groupID` FROM `membership_userrecords` WHERE `tableName`='movement_of_personnel_in_government_garage_and_workshops' AND `pkValue`='" . makeSafe($selected_id) . "'");
		$ownerMemberID = sqlValue("SELECT LCASE(`memberID`) FROM `membership_userrecords` WHERE `tableName`='movement_of_personnel_in_government_garage_and_workshops' AND `pkValue`='" . makeSafe($selected_id) . "'");

		if($arrPerm['view'] == 1 && getLoggedMemberID() != $ownerMemberID) return $Translation['tableAccessDenied'];
		if($arrPerm['view'] == 2 && getLoggedGroupID() != $ownerGroupID) return $Translation['tableAccessDenied'];

		// can edit?
		$AllowUpdate = 0;
		if(($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3) {
			$AllowUpdate = 1;
		}

		$res = sql("SELECT * FROM `movement_of_personnel_in_government_garage_and_workshops` WHERE `movement_id`='" . makeSafe($selected_id) . "'", $eo);
		if(!($row = db_fetch_array($res))) {
			return error_message($Translation['No records found'], 'movement_of_personnel_in_government_garage_and_workshops_view.php', false);
		}
		$combo_vehicle_inspection->SelectedData = $row['vehicle_inspection'];
		$combo_make_of_vehicle->SelectedData = $row['make_of_vehicle'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_vehicle_inspection->SelectedData = $filterer_vehicle_inspection;
		$combo_make_of_vehicle->SelectedData = $filterer_make_of_vehicle;
	}
	$combo_vehicle_inspection->HTML = '<span id="vehicle_inspection-container' . $rnd1 . '"></span><input type="hidden" name="vehicle_inspection" id="vehicle_inspection' . $rnd1 . '" value="' . html_attr($combo_vehicle_inspection->SelectedData) . '">';
	$combo_vehicle_inspection->MatchText = '<span id="vehicle_inspection-container-readonly' . $rnd1 . '"></span><input type="hidden" name="vehicle_inspection" id="vehicle_inspection' . $rnd1 . '" value="' . html_attr($combo_vehicle_inspection->SelectedData) . '">';
	$combo_make_of_vehicle->HTML = '<span id="make_of_vehicle-container' . $rnd1 . '"></span><input type="hidden" name="make_of_vehicle" id="make_of_vehicle' . $rnd1 . '" value="' . html_attr($combo_make_of_vehicle->SelectedData) . '">';
	$combo_make_of_vehicle->MatchText = '<span id="make_of_vehicle-container-readonly' . $rnd1 . '"></span><input type="hidden" name="make_of_vehicle" id="make_of_vehicle' . $rnd1 . '" value="' . html_attr($combo_make_of_vehicle->SelectedData) . '">';

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_vehicle_inspection__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['vehicle_inspection'] : htmlspecialchars($filterer_vehicle_inspection, ENT_QUOTES)); ?>"};
		AppGini.current_make_of_vehicle__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['make_of_vehicle'] : htmlspecialchars($filterer_make_of_vehicle, ENT_QUOTES)); ?>"};

		jQuery(function() {
			setTimeout(function() {
				if(typeof(vehicle_inspection_reload__RAND__) == 'function') vehicle_inspection_reload__RAND__();
				if(typeof(make_of_vehicle_reload__RAND__) == 'function') make_of_vehicle_reload__RAND__();
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function vehicle_inspection_reload__RAND__() {
		<?php if(($AllowUpdate || ($arrPerm['insert'] && !$selected_id)) && !$dvprint) { ?>

			$j("#vehicle_inspection-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_vehicle_inspection__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'vehicle_inspection' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="vehicle_inspection"]').val(resp.results[0].id);
							$j('[id=vehicle_inspection-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_inspection-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').hide(); } else { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').show(); }


							if(typeof(vehicle_inspection_update_autofills__RAND__) == 'function') vehicle_inspection_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'vehicle_inspection' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_vehicle_inspection__RAND__.value = e.added.id;
				AppGini.current_vehicle_inspection__RAND__.text = e.added.text;
				$j('[name="vehicle_inspection"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').hide(); } else { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').show(); }


				if(typeof(vehicle_inspection_update_autofills__RAND__) == 'function') vehicle_inspection_update_autofills__RAND__();
			});

			if(!$j("#vehicle_inspection-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_vehicle_inspection__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'vehicle_inspection' },
					success: function(resp) {
						$j('[name="vehicle_inspection"]').val(resp.results[0].id);
						$j('[id=vehicle_inspection-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_inspection-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').hide(); } else { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').show(); }

						if(typeof(vehicle_inspection_update_autofills__RAND__) == 'function') vehicle_inspection_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_vehicle_inspection__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'vehicle_inspection' },
				success: function(resp) {
					$j('[id=vehicle_inspection-container__RAND__], [id=vehicle_inspection-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_inspection-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').hide(); } else { $j('.btn[id=movement_of_personnel_in_government_garage_and_workshops_view_parent]').show(); }

					if(typeof(vehicle_inspection_update_autofills__RAND__) == 'function') vehicle_inspection_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
		function make_of_vehicle_reload__RAND__() {
		<?php if(($AllowUpdate || ($arrPerm['insert'] && !$selected_id)) && !$dvprint) { ?>

			$j("#make_of_vehicle-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_make_of_vehicle__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'make_of_vehicle' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="make_of_vehicle"]').val(resp.results[0].id);
							$j('[id=make_of_vehicle-container-readonly__RAND__]').html('<span class="match-text" id="make_of_vehicle-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=dealer_view_parent]').hide(); } else { $j('.btn[id=dealer_view_parent]').show(); }


							if(typeof(make_of_vehicle_update_autofills__RAND__) == 'function') make_of_vehicle_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'make_of_vehicle' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_make_of_vehicle__RAND__.value = e.added.id;
				AppGini.current_make_of_vehicle__RAND__.text = e.added.text;
				$j('[name="make_of_vehicle"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=dealer_view_parent]').hide(); } else { $j('.btn[id=dealer_view_parent]').show(); }


				if(typeof(make_of_vehicle_update_autofills__RAND__) == 'function') make_of_vehicle_update_autofills__RAND__();
			});

			if(!$j("#make_of_vehicle-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_make_of_vehicle__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'make_of_vehicle' },
					success: function(resp) {
						$j('[name="make_of_vehicle"]').val(resp.results[0].id);
						$j('[id=make_of_vehicle-container-readonly__RAND__]').html('<span class="match-text" id="make_of_vehicle-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=dealer_view_parent]').hide(); } else { $j('.btn[id=dealer_view_parent]').show(); }

						if(typeof(make_of_vehicle_update_autofills__RAND__) == 'function') make_of_vehicle_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_make_of_vehicle__RAND__.value, t: 'movement_of_personnel_in_government_garage_and_workshops', f: 'make_of_vehicle' },
				success: function(resp) {
					$j('[id=make_of_vehicle-container__RAND__], [id=make_of_vehicle-container-readonly__RAND__]').html('<span class="match-text" id="make_of_vehicle-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=dealer_view_parent]').hide(); } else { $j('.btn[id=dealer_view_parent]').show(); }

					if(typeof(make_of_vehicle_update_autofills__RAND__) == 'function') make_of_vehicle_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$TemplateDVP}") ? "./{$TemplateDVP}" : './templates/movement_of_personnel_in_government_garage_and_workshops_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$TemplateDV}") ? "./{$TemplateDV}" : './templates/movement_of_personnel_in_government_garage_and_workshops_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Movement of Personnel in Government Garage and Workshops ID Details:', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($arrPerm['insert'] && !$selected_id) { // allow insert and no record selected?
		if(!$selected_id) $templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1" onclick="return movement_of_personnel_in_government_garage_and_workshops_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1" onclick="return movement_of_personnel_in_government_garage_and_workshops_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = '$j(\'form\').eq(0).attr(\'novalidate\', \'novalidate\'); document.myform.reset(); return true;';
	}

	if($selected_id) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" onclick="$j(\'form\').eq(0).prop(\'novalidate\', true); document.myform.reset(); return true;" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($AllowUpdate) {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" onclick="return movement_of_personnel_in_government_garage_and_workshops_validateData();" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		}
		if(($arrPerm['delete'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['delete'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['delete'] == 3) { // allow delete?
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" onclick="return confirm(\'' . $Translation['are you sure?'] . '\');" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		}
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$arrPerm['insert']
			&& !$arrPerm['update'] && !$arrPerm['delete'] && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>', 
				'<button
					type="submit" 
					class="btn btn-default" 
					id="deselect" 
					name="deselect_x" 
					value="1" 
					onclick="' . $backAction . '" 
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(($selected_id && !$AllowUpdate) || (!$selected_id && !$AllowInsert)) {
		$jsReadOnly = '';
		$jsReadOnly .= "\tjQuery('#vehicle_inspection').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#vehicle_inspection_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#vehicle_model').replaceWith('<div class=\"form-control-static\" id=\"vehicle_model\">' + (jQuery('#vehicle_model').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_number_plate').replaceWith('<div class=\"form-control-static\" id=\"vehicle_number_plate\">' + (jQuery('#vehicle_number_plate').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_tires_check').replaceWith('<div class=\"form-control-static\" id=\"vehicle_tires_check\">' + (jQuery('#vehicle_tires_check').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_mirrow_check').replaceWith('<div class=\"form-control-static\" id=\"vehicle_mirrow_check\">' + (jQuery('#vehicle_mirrow_check').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#gate_security_signature').replaceWith('<div class=\"form-control-static\" id=\"gate_security_signature\">' + (jQuery('#gate_security_signature').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#government_garage_protocol').replaceWith('<div class=\"form-control-static\" id=\"government_garage_protocol\">' + (jQuery('#government_garage_protocol').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#government_garage_safety').replaceWith('<div class=\"form-control-static\" id=\"government_garage_safety\">' + (jQuery('#government_garage_safety').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_handing_over_checklist').replaceWith('<div class=\"form-control-static\" id=\"vehicle_handing_over_checklist\">' + (jQuery('#vehicle_handing_over_checklist').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_handing_over_checklist, #vehicle_handing_over_checklist-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#vehicle_return_list').replaceWith('<div class=\"form-control-static\" id=\"vehicle_return_list\">' + (jQuery('#vehicle_return_list').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_return_list, #vehicle_return_list-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#approved_workshop_procedure_manual').replaceWith('<div class=\"form-control-static\" id=\"approved_workshop_procedure_manual\">' + (jQuery('#approved_workshop_procedure_manual').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#approved_workshop_procedure_manual, #approved_workshop_procedure_manual-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#vehicle_registration_number').replaceWith('<div class=\"form-control-static\" id=\"vehicle_registration_number\">' + (jQuery('#vehicle_registration_number').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#engine_number').replaceWith('<div class=\"form-control-static\" id=\"engine_number\">' + (jQuery('#engine_number').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#make_of_vehicle').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#make_of_vehicle_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('.select2-container').hide();\n";

		$noUploads = true;
	} elseif(($AllowInsert && !$selected_id) || ($AllowUpdate && $selected_id)) {
		$jsEditable = "\tjQuery('form').eq(0).data('already_changed', true);"; // temporarily disable form change handler
		$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(vehicle_inspection)%%>', $combo_vehicle_inspection->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(vehicle_inspection)%%>', $combo_vehicle_inspection->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(vehicle_inspection)%%>', urlencode($combo_vehicle_inspection->MatchText), $templateCode);
	$templateCode = str_replace('<%%COMBO(make_of_vehicle)%%>', $combo_make_of_vehicle->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(make_of_vehicle)%%>', $combo_make_of_vehicle->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(make_of_vehicle)%%>', urlencode($combo_make_of_vehicle->MatchText), $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = ['vehicle_inspection' => ['movement_of_personnel_in_government_garage_and_workshops', 'Vehicle Inspection:'], 'make_of_vehicle' => ['dealer', 'Make of Vehicle:'], ];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if($pt_perm['view'] || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(movement_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_inspection)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_model)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_number_plate)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_tires_check)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_mirrow_check)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(gate_security_signature)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="gate_security_signature" id="gate_security_signature" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="512000" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(government_garage_protocol)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(government_garage_safety)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_handing_over_checklist)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="vehicle_handing_over_checklist" id="vehicle_handing_over_checklist" data-filetypes="txt|doc|docx|docm|odt|pdf|rtf" data-maxsize="5365760" accept=".txt,.doc,.docx,.docm,.odt,.pdf,.rtf">'), $templateCode);
	if($AllowUpdate && $row['vehicle_handing_over_checklist'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(vehicle_handing_over_checklist)%%>', '<br><input type="checkbox" name="vehicle_handing_over_checklist_remove" id="vehicle_handing_over_checklist_remove" value="1"> <label for="vehicle_handing_over_checklist_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(vehicle_handing_over_checklist)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_return_list)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="vehicle_return_list" id="vehicle_return_list" data-filetypes="txt|doc|docx|docm|odt|pdf|rtf" data-maxsize="5365760" accept=".txt,.doc,.docx,.docm,.odt,.pdf,.rtf">'), $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(approved_workshop_procedure_manual)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="approved_workshop_procedure_manual" id="approved_workshop_procedure_manual" data-filetypes="txt|doc|docx|docm|odt|pdf|rtf" data-maxsize="5365760" accept=".txt,.doc,.docx,.docm,.odt,.pdf,.rtf">'), $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_registration_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(engine_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(make_of_vehicle)%%>', '', $templateCode);

	// process values
	if($selected_id) {
		if( $dvprint) $templateCode = str_replace('<%%VALUE(movement_id)%%>', safe_html($urow['movement_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(movement_id)%%>', html_attr($row['movement_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(movement_id)%%>', urlencode($urow['movement_id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_inspection)%%>', safe_html($urow['vehicle_inspection']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_inspection)%%>', html_attr($row['vehicle_inspection']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_inspection)%%>', urlencode($urow['vehicle_inspection']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_model)%%>', safe_html($urow['vehicle_model']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_model)%%>', html_attr($row['vehicle_model']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_model)%%>', urlencode($urow['vehicle_model']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_number_plate)%%>', safe_html($urow['vehicle_number_plate']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_number_plate)%%>', html_attr($row['vehicle_number_plate']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_number_plate)%%>', urlencode($urow['vehicle_number_plate']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_tires_check)%%>', safe_html($urow['vehicle_tires_check']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_tires_check)%%>', html_attr($row['vehicle_tires_check']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_tires_check)%%>', urlencode($urow['vehicle_tires_check']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_mirrow_check)%%>', safe_html($urow['vehicle_mirrow_check']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_mirrow_check)%%>', html_attr($row['vehicle_mirrow_check']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_mirrow_check)%%>', urlencode($urow['vehicle_mirrow_check']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(gate_security_signature)%%>', safe_html($urow['gate_security_signature']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(gate_security_signature)%%>', html_attr($row['gate_security_signature']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(gate_security_signature)%%>', urlencode($urow['gate_security_signature']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(government_garage_protocol)%%>', safe_html($urow['government_garage_protocol']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(government_garage_protocol)%%>', html_attr($row['government_garage_protocol']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(government_garage_protocol)%%>', urlencode($urow['government_garage_protocol']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(government_garage_safety)%%>', safe_html($urow['government_garage_safety']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(government_garage_safety)%%>', html_attr($row['government_garage_safety']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(government_garage_safety)%%>', urlencode($urow['government_garage_safety']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_handing_over_checklist)%%>', safe_html($urow['vehicle_handing_over_checklist']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_handing_over_checklist)%%>', html_attr($row['vehicle_handing_over_checklist']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_handing_over_checklist)%%>', urlencode($urow['vehicle_handing_over_checklist']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_return_list)%%>', safe_html($urow['vehicle_return_list']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_return_list)%%>', html_attr($row['vehicle_return_list']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_list)%%>', urlencode($urow['vehicle_return_list']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(approved_workshop_procedure_manual)%%>', safe_html($urow['approved_workshop_procedure_manual']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(approved_workshop_procedure_manual)%%>', html_attr($row['approved_workshop_procedure_manual']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(approved_workshop_procedure_manual)%%>', urlencode($urow['approved_workshop_procedure_manual']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', safe_html($urow['vehicle_registration_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', html_attr($row['vehicle_registration_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_registration_number)%%>', urlencode($urow['vehicle_registration_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(engine_number)%%>', safe_html($urow['engine_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(engine_number)%%>', html_attr($row['engine_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(engine_number)%%>', urlencode($urow['engine_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(make_of_vehicle)%%>', safe_html($urow['make_of_vehicle']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(make_of_vehicle)%%>', html_attr($row['make_of_vehicle']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(make_of_vehicle)%%>', urlencode($urow['make_of_vehicle']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(movement_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(movement_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_inspection)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_inspection)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_model)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_model)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_number_plate)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_number_plate)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_tires_check)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_tires_check)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_mirrow_check)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_mirrow_check)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(gate_security_signature)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(gate_security_signature)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(government_garage_protocol)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(government_garage_protocol)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(government_garage_safety)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(government_garage_safety)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_handing_over_checklist)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_handing_over_checklist)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_return_list)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_list)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(approved_workshop_procedure_manual)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(approved_workshop_procedure_manual)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_registration_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(engine_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(engine_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(make_of_vehicle)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(make_of_vehicle)%%>', urlencode(''), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$selected_id) {
			$templateCode.="\n\tif(document.getElementById('vehicle_handing_over_checklistEdit')) { document.getElementById('vehicle_handing_over_checklistEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('vehicle_handing_over_checklistEditLink')) { document.getElementById('vehicle_handing_over_checklistEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('vehicle_return_listEdit')) { document.getElementById('vehicle_return_listEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('vehicle_return_listEditLink')) { document.getElementById('vehicle_return_listEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('approved_workshop_procedure_manualEdit')) { document.getElementById('approved_workshop_procedure_manualEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('approved_workshop_procedure_manualEditLink')) { document.getElementById('approved_workshop_procedure_manualEditLink').style.display='none'; }";
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('movement_of_personnel_in_government_garage_and_workshops');
	if($selected_id) {
		$jdata = get_joined_record('movement_of_personnel_in_government_garage_and_workshops', $selected_id);
		if($jdata === false) $jdata = get_defaults('movement_of_personnel_in_government_garage_and_workshops');
		$rdata = $row;
	}
	$templateCode .= loadView('movement_of_personnel_in_government_garage_and_workshops-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: movement_of_personnel_in_government_garage_and_workshops_dv
	if(function_exists('movement_of_personnel_in_government_garage_and_workshops_dv')) {
		$args = [];
		movement_of_personnel_in_government_garage_and_workshops_dv(($selected_id ? $selected_id : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}