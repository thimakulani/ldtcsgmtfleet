<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/cost_centre.php');
	include_once(__DIR__ . '/cost_centre_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('cost_centre');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'cost_centre';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`cost_centre`.`cost_centre_id`" => "cost_centre_id",
		"`cost_centre`.`cost_centre`" => "cost_centre",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`cost_centre`.`cost_centre_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`cost_centre`.`cost_centre_id`" => "cost_centre_id",
		"`cost_centre`.`cost_centre`" => "cost_centre",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`cost_centre`.`cost_centre_id`" => "Cost Centre ID:",
		"`cost_centre`.`cost_centre`" => "Cost Centre:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`cost_centre`.`cost_centre_id`" => "cost_centre_id",
		"`cost_centre`.`cost_centre`" => "cost_centre",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`cost_centre` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'cost_centre_view.php';
	$x->RedirectAfterInsert = 'cost_centre_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Cost Centre:';
	$x->TableIcon = 'resources/table_icons/school_128.png';
	$x->PrimaryKey = '`cost_centre`.`cost_centre_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Cost Centre ID:', 'Cost Centre:', ];
	$x->ColFieldName = ['cost_centre_id', 'cost_centre', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/cost_centre_templateTV.html';
	$x->SelectedTemplate = 'templates/cost_centre_templateTVS.html';
	$x->TemplateDV = 'templates/cost_centre_templateDV.html';
	$x->TemplateDVP = 'templates/cost_centre_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: cost_centre_init
	$render = true;
	if(function_exists('cost_centre_init')) {
		$args = [];
		$render = cost_centre_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: cost_centre_header
	$headerCode = '';
	if(function_exists('cost_centre_header')) {
		$args = [];
		$headerCode = cost_centre_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: cost_centre_footer
	$footerCode = '';
	if(function_exists('cost_centre_footer')) {
		$args = [];
		$footerCode = cost_centre_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
