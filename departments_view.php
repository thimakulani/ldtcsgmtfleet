<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/departments.php');
	include_once(__DIR__ . '/departments_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('departments');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'departments';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`departments`.`department_id`" => "department_id",
		"`departments`.`department_name`" => "department_name",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`departments`.`department_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`departments`.`department_id`" => "department_id",
		"`departments`.`department_name`" => "department_name",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`departments`.`department_id`" => "Department ID:",
		"`departments`.`department_name`" => "Department Name:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`departments`.`department_id`" => "department_id",
		"`departments`.`department_name`" => "department_name",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`departments` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'departments_view.php';
	$x->RedirectAfterInsert = 'departments_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Departments:';
	$x->TableIcon = 'resources/table_icons/Contacts White.png';
	$x->PrimaryKey = '`departments`.`department_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Department ID:', 'Department Name:', ];
	$x->ColFieldName = ['department_id', 'department_name', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/departments_templateTV.html';
	$x->SelectedTemplate = 'templates/departments_templateTVS.html';
	$x->TemplateDV = 'templates/departments_templateDV.html';
	$x->TemplateDVP = 'templates/departments_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: departments_init
	$render = true;
	if(function_exists('departments_init')) {
		$args = [];
		$render = departments_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: departments_header
	$headerCode = '';
	if(function_exists('departments_header')) {
		$args = [];
		$headerCode = departments_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: departments_footer
	$footerCode = '';
	if(function_exists('departments_footer')) {
		$args = [];
		$footerCode = departments_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
