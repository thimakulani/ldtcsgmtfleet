<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/claim_category.php');
	include_once(__DIR__ . '/claim_category_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('claim_category');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'claim_category';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`claim_category`.`claim_category_id`" => "claim_category_id",
		"`claim_category`.`claim_category`" => "claim_category",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`claim_category`.`claim_category_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`claim_category`.`claim_category_id`" => "claim_category_id",
		"`claim_category`.`claim_category`" => "claim_category",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`claim_category`.`claim_category_id`" => "Claims Category ID:",
		"`claim_category`.`claim_category`" => "Claims Category:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`claim_category`.`claim_category_id`" => "claim_category_id",
		"`claim_category`.`claim_category`" => "claim_category",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`claim_category` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'claim_category_view.php';
	$x->RedirectAfterInsert = 'claim_category_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Claims Category:';
	$x->TableIcon = 'resources/table_icons/Internet-bags-icon.png';
	$x->PrimaryKey = '`claim_category`.`claim_category_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Claims Category ID:', 'Claims Category:', ];
	$x->ColFieldName = ['claim_category_id', 'claim_category', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/claim_category_templateTV.html';
	$x->SelectedTemplate = 'templates/claim_category_templateTVS.html';
	$x->TemplateDV = 'templates/claim_category_templateDV.html';
	$x->TemplateDVP = 'templates/claim_category_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: claim_category_init
	$render = true;
	if(function_exists('claim_category_init')) {
		$args = [];
		$render = claim_category_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: claim_category_header
	$headerCode = '';
	if(function_exists('claim_category_header')) {
		$args = [];
		$headerCode = claim_category_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: claim_category_footer
	$footerCode = '';
	if(function_exists('claim_category_footer')) {
		$args = [];
		$footerCode = claim_category_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
