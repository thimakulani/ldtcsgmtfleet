<?php

// Data functions (insert, update, delete, form) for table indicates_repair_damages_found_list

// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

function indicates_repair_damages_found_list_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('indicates_repair_damages_found_list');
	if(!$arrPerm['insert']) return false;

	$data = [
		'brought_in_for_repairs' => Request::val('brought_in_for_repairs', ''),
		'after_repairs' => Request::val('after_repairs', ''),
		'driver_name_and_surname' => Request::lookup('driver_name_and_surname', ''),
		'driver_persal_number' => Request::lookup('driver_name_and_surname'),
		'driver_signature' => Request::fileUpload('driver_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'success' => function($name, $selected_id) {
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'driver_signature', 'tv'));
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'driver_signature', 'dv'));
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('indicates_repair_damages_found_list', 'driver_signature', Request::val('SelectedID'));
			},
		]),
		'vehicle_return_date_signed' => mysql_datetime(Request::val('vehicle_return_date_signed', '2020-01-01 00:00:00')),
		'company_name_and_surname' => Request::val('company_name_and_surname', ''),
		'company_repesentative_signature' => Request::fileUpload('company_repesentative_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'success' => function($name, $selected_id) {
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'company_repesentative_signature', 'tv'));
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'company_repesentative_signature', 'dv'));
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('indicates_repair_damages_found_list', 'company_repesentative_signature', Request::val('SelectedID'));
			},
		]),
		'vehicle_return_date_signed_by_representative' => mysql_datetime(Request::val('vehicle_return_date_signed_by_representative', '2020-01-01 00:00:00')),
		'indicates_and_list_details_of_damages_deficiencies' => Request::fileUpload('indicates_and_list_details_of_damages_deficiencies', [
			'maxSize' => 2097152,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('indicates_repair_damages_found_list', 'indicates_and_list_details_of_damages_deficiencies', Request::val('SelectedID'));
			},
		]),
	];


	// hook: indicates_repair_damages_found_list_before_insert
	if(function_exists('indicates_repair_damages_found_list_before_insert')) {
		$args = [];
		if(!indicates_repair_damages_found_list_before_insert($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$error = '';
	// set empty fields to NULL
	$data = array_map(function($v) { return ($v === '' ? NULL : $v); }, $data);
	insert('indicates_repair_damages_found_list', backtick_keys_once($data), $error);
	if($error) {
		$error_message = $error;
		return false;
	}

	$recID = db_insert_id(db_link());

	update_calc_fields('indicates_repair_damages_found_list', $recID, calculated_fields()['indicates_repair_damages_found_list']);

	// hook: indicates_repair_damages_found_list_after_insert
	if(function_exists('indicates_repair_damages_found_list_after_insert')) {
		$res = sql("SELECT * FROM `indicates_repair_damages_found_list` WHERE `repair_damages_list_id`='" . makeSafe($recID, false) . "' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) {
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = makeSafe($recID, false);
		$args = [];
		if(!indicates_repair_damages_found_list_after_insert($data, getMemberInfo(), $args)) { return $recID; }
	}

	// mm: save ownership data
	set_record_owner('indicates_repair_damages_found_list', $recID, getLoggedMemberID());

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID'))) indicates_repair_damages_found_list_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function indicates_repair_damages_found_list_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);

	// launch requests, asynchronously
	curl_batch($requests);
}

function indicates_repair_damages_found_list_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('indicates_repair_damages_found_list', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: indicates_repair_damages_found_list_before_delete
	if(function_exists('indicates_repair_damages_found_list_before_delete')) {
		$args = [];
		if(!indicates_repair_damages_found_list_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: '' 
			);
	}

	sql("DELETE FROM `indicates_repair_damages_found_list` WHERE `repair_damages_list_id`='{$selected_id}'", $eo);

	// hook: indicates_repair_damages_found_list_after_delete
	if(function_exists('indicates_repair_damages_found_list_after_delete')) {
		$args = [];
		indicates_repair_damages_found_list_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='indicates_repair_damages_found_list' AND `pkValue`='{$selected_id}'", $eo);
}

function indicates_repair_damages_found_list_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('indicates_repair_damages_found_list', $selected_id, 'edit')) return false;

	$data = [
		'brought_in_for_repairs' => Request::val('brought_in_for_repairs', ''),
		'after_repairs' => Request::val('after_repairs', ''),
		'driver_name_and_surname' => Request::lookup('driver_name_and_surname', ''),
		'driver_persal_number' => Request::lookup('driver_name_and_surname'),
		'driver_signature' => Request::fileUpload('driver_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'driver_signature', 'tv'));
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'driver_signature', 'dv'));
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('indicates_repair_damages_found_list', 'driver_signature', $selected_id);
			},
		]),
		'vehicle_return_date_signed' => mysql_datetime(Request::val('vehicle_return_date_signed', '')),
		'company_name_and_surname' => Request::val('company_name_and_surname', ''),
		'company_repesentative_signature' => Request::fileUpload('company_repesentative_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'company_repesentative_signature', 'tv'));
				createThumbnail($name, getThumbnailSpecs('indicates_repair_damages_found_list', 'company_repesentative_signature', 'dv'));
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('indicates_repair_damages_found_list', 'company_repesentative_signature', $selected_id);
			},
		]),
		'vehicle_return_date_signed_by_representative' => mysql_datetime(Request::val('vehicle_return_date_signed_by_representative', '')),
		'indicates_and_list_details_of_damages_deficiencies' => Request::fileUpload('indicates_and_list_details_of_damages_deficiencies', [
			'maxSize' => 2097152,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('indicates_repair_damages_found_list', 'indicates_and_list_details_of_damages_deficiencies', $selected_id);
			},
		]),
	];

	// get existing values
	$old_data = getRecord('indicates_repair_damages_found_list', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: indicates_repair_damages_found_list_before_update
	if(function_exists('indicates_repair_damages_found_list_before_update')) {
		$args = ['old_data' => $old_data];
		if(!indicates_repair_damages_found_list_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'indicates_repair_damages_found_list', 
		backtick_keys_once($set), 
		['`repair_damages_list_id`' => $selected_id], 
		$error_message
	)) {
		echo $error_message;
		echo '<a href="indicates_repair_damages_found_list_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	$eo = ['silentErrors' => true];

	update_calc_fields('indicates_repair_damages_found_list', $data['selectedID'], calculated_fields()['indicates_repair_damages_found_list']);

	// hook: indicates_repair_damages_found_list_after_update
	if(function_exists('indicates_repair_damages_found_list_after_update')) {
		$res = sql("SELECT * FROM `indicates_repair_damages_found_list` WHERE `repair_damages_list_id`='{$data['selectedID']}' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['repair_damages_list_id'];
		$args = ['old_data' => $old_data];
		if(!indicates_repair_damages_found_list_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update ownership data
	sql("UPDATE `membership_userrecords` SET `dateUpdated`='" . time() . "' WHERE `tableName`='indicates_repair_damages_found_list' AND `pkValue`='" . makeSafe($selected_id) . "'", $eo);
}

function indicates_repair_damages_found_list_form($selected_id = '', $AllowUpdate = 1, $AllowInsert = 1, $AllowDelete = 1, $separateDV = 0, $TemplateDV = '', $TemplateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selected_id. If $selected_id
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = null;
	$row = $urow = $jsReadOnly = $jsEditable = $lookups = null;

	$noSaveAsCopy = true;

	// mm: get table permissions
	$arrPerm = getTablePermissions('indicates_repair_damages_found_list');
	if(!$arrPerm['insert'] && $selected_id == '')
		// no insert permission and no record selected
		// so show access denied error unless TVDV
		return $separateDV ? $Translation['tableAccessDenied'] : '';
	$AllowInsert = ($arrPerm['insert'] ? true : false);
	// print preview?
	$dvprint = false;
	if(strlen($selected_id) && Request::val('dvprint_x') != '') {
		$dvprint = true;
	}

	$filterer_driver_name_and_surname = Request::val('filterer_driver_name_and_surname');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: driver_name_and_surname
	$combo_driver_name_and_surname = new DataCombo;

	if($selected_id) {
		// mm: check member permissions
		if(!$arrPerm['view']) return $Translation['tableAccessDenied'];

		// mm: who is the owner?
		$ownerGroupID = sqlValue("SELECT `groupID` FROM `membership_userrecords` WHERE `tableName`='indicates_repair_damages_found_list' AND `pkValue`='" . makeSafe($selected_id) . "'");
		$ownerMemberID = sqlValue("SELECT LCASE(`memberID`) FROM `membership_userrecords` WHERE `tableName`='indicates_repair_damages_found_list' AND `pkValue`='" . makeSafe($selected_id) . "'");

		if($arrPerm['view'] == 1 && getLoggedMemberID() != $ownerMemberID) return $Translation['tableAccessDenied'];
		if($arrPerm['view'] == 2 && getLoggedGroupID() != $ownerGroupID) return $Translation['tableAccessDenied'];

		// can edit?
		$AllowUpdate = 0;
		if(($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3) {
			$AllowUpdate = 1;
		}

		$res = sql("SELECT * FROM `indicates_repair_damages_found_list` WHERE `repair_damages_list_id`='" . makeSafe($selected_id) . "'", $eo);
		if(!($row = db_fetch_array($res))) {
			return error_message($Translation['No records found'], 'indicates_repair_damages_found_list_view.php', false);
		}
		$combo_driver_name_and_surname->SelectedData = $row['driver_name_and_surname'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_driver_name_and_surname->SelectedData = $filterer_driver_name_and_surname;
	}
	$combo_driver_name_and_surname->HTML = '<span id="driver_name_and_surname-container' . $rnd1 . '"></span><input type="hidden" name="driver_name_and_surname" id="driver_name_and_surname' . $rnd1 . '" value="' . html_attr($combo_driver_name_and_surname->SelectedData) . '">';
	$combo_driver_name_and_surname->MatchText = '<span id="driver_name_and_surname-container-readonly' . $rnd1 . '"></span><input type="hidden" name="driver_name_and_surname" id="driver_name_and_surname' . $rnd1 . '" value="' . html_attr($combo_driver_name_and_surname->SelectedData) . '">';

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_driver_name_and_surname__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['driver_name_and_surname'] : htmlspecialchars($filterer_driver_name_and_surname, ENT_QUOTES)); ?>"};

		jQuery(function() {
			setTimeout(function() {
				if(typeof(driver_name_and_surname_reload__RAND__) == 'function') driver_name_and_surname_reload__RAND__();
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function driver_name_and_surname_reload__RAND__() {
		<?php if(($AllowUpdate || ($arrPerm['insert'] && !$selected_id)) && !$dvprint) { ?>

			$j("#driver_name_and_surname-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_driver_name_and_surname__RAND__.value, t: 'indicates_repair_damages_found_list', f: 'driver_name_and_surname' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="driver_name_and_surname"]').val(resp.results[0].id);
							$j('[id=driver_name_and_surname-container-readonly__RAND__]').html('<span class="match-text" id="driver_name_and_surname-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=driver_view_parent]').hide(); } else { $j('.btn[id=driver_view_parent]').show(); }


							if(typeof(driver_name_and_surname_update_autofills__RAND__) == 'function') driver_name_and_surname_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'indicates_repair_damages_found_list', f: 'driver_name_and_surname' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_driver_name_and_surname__RAND__.value = e.added.id;
				AppGini.current_driver_name_and_surname__RAND__.text = e.added.text;
				$j('[name="driver_name_and_surname"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=driver_view_parent]').hide(); } else { $j('.btn[id=driver_view_parent]').show(); }


				if(typeof(driver_name_and_surname_update_autofills__RAND__) == 'function') driver_name_and_surname_update_autofills__RAND__();
			});

			if(!$j("#driver_name_and_surname-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_driver_name_and_surname__RAND__.value, t: 'indicates_repair_damages_found_list', f: 'driver_name_and_surname' },
					success: function(resp) {
						$j('[name="driver_name_and_surname"]').val(resp.results[0].id);
						$j('[id=driver_name_and_surname-container-readonly__RAND__]').html('<span class="match-text" id="driver_name_and_surname-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=driver_view_parent]').hide(); } else { $j('.btn[id=driver_view_parent]').show(); }

						if(typeof(driver_name_and_surname_update_autofills__RAND__) == 'function') driver_name_and_surname_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_driver_name_and_surname__RAND__.value, t: 'indicates_repair_damages_found_list', f: 'driver_name_and_surname' },
				success: function(resp) {
					$j('[id=driver_name_and_surname-container__RAND__], [id=driver_name_and_surname-container-readonly__RAND__]').html('<span class="match-text" id="driver_name_and_surname-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=driver_view_parent]').hide(); } else { $j('.btn[id=driver_view_parent]').show(); }

					if(typeof(driver_name_and_surname_update_autofills__RAND__) == 'function') driver_name_and_surname_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$TemplateDVP}") ? "./{$TemplateDVP}" : './templates/indicates_repair_damages_found_list_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$TemplateDV}") ? "./{$TemplateDV}" : './templates/indicates_repair_damages_found_list_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Indicates Repair and Damages Found Details:', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($arrPerm['insert'] && !$selected_id) { // allow insert and no record selected?
		if(!$selected_id) $templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1" onclick="return indicates_repair_damages_found_list_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1" onclick="return indicates_repair_damages_found_list_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = '$j(\'form\').eq(0).attr(\'novalidate\', \'novalidate\'); document.myform.reset(); return true;';
	}

	if($selected_id) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" onclick="$j(\'form\').eq(0).prop(\'novalidate\', true); document.myform.reset(); return true;" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($AllowUpdate) {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" onclick="return indicates_repair_damages_found_list_validateData();" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		}
		if(($arrPerm['delete'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['delete'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['delete'] == 3) { // allow delete?
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" onclick="return confirm(\'' . $Translation['are you sure?'] . '\');" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		}
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$arrPerm['insert']
			&& !$arrPerm['update'] && !$arrPerm['delete'] && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>', 
				'<button
					type="submit" 
					class="btn btn-default" 
					id="deselect" 
					name="deselect_x" 
					value="1" 
					onclick="' . $backAction . '" 
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(($selected_id && !$AllowUpdate) || (!$selected_id && !$AllowInsert)) {
		$jsReadOnly = '';
		$jsReadOnly .= "\tjQuery('#driver_name_and_surname').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#driver_name_and_surname_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#driver_signature').replaceWith('<div class=\"form-control-static\" id=\"driver_signature\">' + (jQuery('#driver_signature').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_return_date_signed').parents('.input-group').replaceWith('<div class=\"form-control-static\" id=\"vehicle_return_date_signed\">' + (jQuery('#vehicle_return_date_signed').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#company_name_and_surname').replaceWith('<div class=\"form-control-static\" id=\"company_name_and_surname\">' + (jQuery('#company_name_and_surname').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#company_repesentative_signature').replaceWith('<div class=\"form-control-static\" id=\"company_repesentative_signature\">' + (jQuery('#company_repesentative_signature').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_return_date_signed_by_representative').parents('.input-group').replaceWith('<div class=\"form-control-static\" id=\"vehicle_return_date_signed_by_representative\">' + (jQuery('#vehicle_return_date_signed_by_representative').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#indicates_and_list_details_of_damages_deficiencies').replaceWith('<div class=\"form-control-static\" id=\"indicates_and_list_details_of_damages_deficiencies\">' + (jQuery('#indicates_and_list_details_of_damages_deficiencies').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#indicates_and_list_details_of_damages_deficiencies, #indicates_and_list_details_of_damages_deficiencies-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('.select2-container').hide();\n";

		$noUploads = true;
	} elseif(($AllowInsert && !$selected_id) || ($AllowUpdate && $selected_id)) {
		$jsEditable = "\tjQuery('form').eq(0).data('already_changed', true);"; // temporarily disable form change handler
		$locale = isset($Translation['datetimepicker locale']) ? ", locale: '{$Translation['datetimepicker locale']}'" : '';
		$jsEditable .= "\tjQuery('#vehicle_return_date_signed').addClass('always_shown').parents('.input-group').datetimepicker({ toolbarPlacement: 'top', sideBySide: true, showClear: true, showTodayButton: true, showClose: true, icons: { close: 'glyphicon glyphicon-ok' }, format: AppGini.datetimeFormat('dt') {$locale} });";
		$locale = isset($Translation['datetimepicker locale']) ? ", locale: '{$Translation['datetimepicker locale']}'" : '';
		$jsEditable .= "\tjQuery('#vehicle_return_date_signed_by_representative').addClass('always_shown').parents('.input-group').datetimepicker({ toolbarPlacement: 'top', sideBySide: true, showClear: true, showTodayButton: true, showClose: true, icons: { close: 'glyphicon glyphicon-ok' }, format: AppGini.datetimeFormat('dt') {$locale} });";
		$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(driver_name_and_surname)%%>', $combo_driver_name_and_surname->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(driver_name_and_surname)%%>', $combo_driver_name_and_surname->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(driver_name_and_surname)%%>', urlencode($combo_driver_name_and_surname->MatchText), $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = ['driver_name_and_surname' => ['driver', 'Driver Name & Surname:'], ];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if($pt_perm['view'] || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(repair_damages_list_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(brought_in_for_repairs)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(after_repairs)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(driver_name_and_surname)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(driver_signature)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="driver_signature" id="driver_signature" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="512000" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	if($AllowUpdate && $row['driver_signature'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(driver_signature)%%>', '<br><input type="checkbox" name="driver_signature_remove" id="driver_signature_remove" value="1"> <label for="driver_signature_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(driver_signature)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_return_date_signed)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(company_name_and_surname)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(company_repesentative_signature)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="company_repesentative_signature" id="company_repesentative_signature" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="512000" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	if($AllowUpdate && $row['company_repesentative_signature'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(company_repesentative_signature)%%>', '<br><input type="checkbox" name="company_repesentative_signature_remove" id="company_repesentative_signature_remove" value="1"> <label for="company_repesentative_signature_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(company_repesentative_signature)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_return_date_signed_by_representative)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(indicates_and_list_details_of_damages_deficiencies)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="indicates_and_list_details_of_damages_deficiencies" id="indicates_and_list_details_of_damages_deficiencies" data-filetypes="txt|doc|docx|docm|odt|pdf|rtf" data-maxsize="2097152" accept=".txt,.doc,.docx,.docm,.odt,.pdf,.rtf">'), $templateCode);
	if($AllowUpdate && $row['indicates_and_list_details_of_damages_deficiencies'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(indicates_and_list_details_of_damages_deficiencies)%%>', '<br><input type="checkbox" name="indicates_and_list_details_of_damages_deficiencies_remove" id="indicates_and_list_details_of_damages_deficiencies_remove" value="1"> <label for="indicates_and_list_details_of_damages_deficiencies_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(indicates_and_list_details_of_damages_deficiencies)%%>', '', $templateCode);
	}

	// process values
	if($selected_id) {
		if( $dvprint) $templateCode = str_replace('<%%VALUE(repair_damages_list_id)%%>', safe_html($urow['repair_damages_list_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(repair_damages_list_id)%%>', html_attr($row['repair_damages_list_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(repair_damages_list_id)%%>', urlencode($urow['repair_damages_list_id']), $templateCode);
		if($AllowUpdate || $AllowInsert) {
			$templateCode = str_replace('<%%HTMLAREA(brought_in_for_repairs)%%>', '<textarea name="brought_in_for_repairs" id="brought_in_for_repairs" rows="5">' . safe_html(htmlspecialchars_decode($row['brought_in_for_repairs'])) . '</textarea>', $templateCode);
		} else {
			$templateCode = str_replace('<%%HTMLAREA(brought_in_for_repairs)%%>', '<div id="brought_in_for_repairs" class="form-control-static">' . $row['brought_in_for_repairs'] . '</div>', $templateCode);
		}
		$templateCode = str_replace('<%%VALUE(brought_in_for_repairs)%%>', nl2br($row['brought_in_for_repairs']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(brought_in_for_repairs)%%>', urlencode($urow['brought_in_for_repairs']), $templateCode);
		if($AllowUpdate || $AllowInsert) {
			$templateCode = str_replace('<%%HTMLAREA(after_repairs)%%>', '<textarea name="after_repairs" id="after_repairs" rows="5">' . safe_html(htmlspecialchars_decode($row['after_repairs'])) . '</textarea>', $templateCode);
		} else {
			$templateCode = str_replace('<%%HTMLAREA(after_repairs)%%>', '<div id="after_repairs" class="form-control-static">' . $row['after_repairs'] . '</div>', $templateCode);
		}
		$templateCode = str_replace('<%%VALUE(after_repairs)%%>', nl2br($row['after_repairs']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(after_repairs)%%>', urlencode($urow['after_repairs']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(driver_name_and_surname)%%>', safe_html($urow['driver_name_and_surname']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(driver_name_and_surname)%%>', html_attr($row['driver_name_and_surname']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(driver_name_and_surname)%%>', urlencode($urow['driver_name_and_surname']), $templateCode);
		$row['driver_signature'] = ($row['driver_signature'] != '' ? $row['driver_signature'] : 'blank.gif');
		if( $dvprint) $templateCode = str_replace('<%%VALUE(driver_signature)%%>', safe_html($urow['driver_signature']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(driver_signature)%%>', html_attr($row['driver_signature']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(driver_signature)%%>', urlencode($urow['driver_signature']), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_return_date_signed)%%>', app_datetime($row['vehicle_return_date_signed'], 'dt'), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_date_signed)%%>', urlencode(app_datetime($urow['vehicle_return_date_signed'], 'dt')), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(company_name_and_surname)%%>', safe_html($urow['company_name_and_surname']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(company_name_and_surname)%%>', html_attr($row['company_name_and_surname']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(company_name_and_surname)%%>', urlencode($urow['company_name_and_surname']), $templateCode);
		$row['company_repesentative_signature'] = ($row['company_repesentative_signature'] != '' ? $row['company_repesentative_signature'] : 'blank.gif');
		if( $dvprint) $templateCode = str_replace('<%%VALUE(company_repesentative_signature)%%>', safe_html($urow['company_repesentative_signature']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(company_repesentative_signature)%%>', html_attr($row['company_repesentative_signature']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(company_repesentative_signature)%%>', urlencode($urow['company_repesentative_signature']), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_return_date_signed_by_representative)%%>', app_datetime($row['vehicle_return_date_signed_by_representative'], 'dt'), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_date_signed_by_representative)%%>', urlencode(app_datetime($urow['vehicle_return_date_signed_by_representative'], 'dt')), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(indicates_and_list_details_of_damages_deficiencies)%%>', safe_html($urow['indicates_and_list_details_of_damages_deficiencies']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(indicates_and_list_details_of_damages_deficiencies)%%>', html_attr($row['indicates_and_list_details_of_damages_deficiencies']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(indicates_and_list_details_of_damages_deficiencies)%%>', urlencode($urow['indicates_and_list_details_of_damages_deficiencies']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(repair_damages_list_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(repair_damages_list_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%HTMLAREA(brought_in_for_repairs)%%>', '<textarea name="brought_in_for_repairs" id="brought_in_for_repairs" rows="5"></textarea>', $templateCode);
		$templateCode = str_replace('<%%HTMLAREA(after_repairs)%%>', '<textarea name="after_repairs" id="after_repairs" rows="5"></textarea>', $templateCode);
		$templateCode = str_replace('<%%VALUE(driver_name_and_surname)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(driver_name_and_surname)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(driver_signature)%%>', 'blank.gif', $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_return_date_signed)%%>', '2020-01-01 00:00:00', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_date_signed)%%>', urlencode('2020-01-01 00:00:00'), $templateCode);
		$templateCode = str_replace('<%%VALUE(company_name_and_surname)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(company_name_and_surname)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(company_repesentative_signature)%%>', 'blank.gif', $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_return_date_signed_by_representative)%%>', '2020-01-01 00:00:00', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_return_date_signed_by_representative)%%>', urlencode('2020-01-01 00:00:00'), $templateCode);
		$templateCode = str_replace('<%%VALUE(indicates_and_list_details_of_damages_deficiencies)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(indicates_and_list_details_of_damages_deficiencies)%%>', urlencode(''), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$selected_id) {
			$templateCode.="\n\tif(document.getElementById('indicates_and_list_details_of_damages_deficienciesEdit')) { document.getElementById('indicates_and_list_details_of_damages_deficienciesEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('indicates_and_list_details_of_damages_deficienciesEditLink')) { document.getElementById('indicates_and_list_details_of_damages_deficienciesEditLink').style.display='none'; }";
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';

	$templateCode .= "\tdriver_name_and_surname_update_autofills$rnd1 = function() {\n";
	$templateCode .= "\t\t\$j.ajax({\n";
	if($dvprint) {
		$templateCode .= "\t\t\turl: 'indicates_repair_damages_found_list_autofill.php?rnd1=$rnd1&mfk=driver_name_and_surname&id=' + encodeURIComponent('".addslashes($row['driver_name_and_surname'])."'),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET'\n";
	} else {
		$templateCode .= "\t\t\turl: 'indicates_repair_damages_found_list_autofill.php?rnd1=$rnd1&mfk=driver_name_and_surname&id=' + encodeURIComponent(AppGini.current_driver_name_and_surname{$rnd1}.value),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET',\n";
		$templateCode .= "\t\t\tbeforeSend: function() { \$j('#driver_name_and_surname$rnd1').prop('disabled', true); },\n";
		$templateCode .= "\t\t\tcomplete: function() { " . (($arrPerm['insert'] || (($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3)) ? "\$j('#driver_name_and_surname$rnd1').prop('disabled', false); " : "\$j('#driver_name_and_surname$rnd1').prop('disabled', true); ")." \$j(window).resize(); }\n";
	}
	$templateCode .= "\t\t});\n";
	$templateCode .= "\t};\n";
	if(!$dvprint) $templateCode .= "\tif(\$j('#driver_name_and_surname_caption').length) \$j('#driver_name_and_surname_caption').click(function() { driver_name_and_surname_update_autofills$rnd1(); });\n";


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('indicates_repair_damages_found_list');
	if($selected_id) {
		$jdata = get_joined_record('indicates_repair_damages_found_list', $selected_id);
		if($jdata === false) $jdata = get_defaults('indicates_repair_damages_found_list');
		$rdata = $row;
	}
	$templateCode .= loadView('indicates_repair_damages_found_list-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: indicates_repair_damages_found_list_dv
	if(function_exists('indicates_repair_damages_found_list_dv')) {
		$args = [];
		indicates_repair_damages_found_list_dv(($selected_id ? $selected_id : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}