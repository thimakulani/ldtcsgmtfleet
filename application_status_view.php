<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/application_status.php');
	include_once(__DIR__ . '/application_status_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('application_status');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'application_status';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`application_status`.`application_id`" => "application_id",
		"`application_status`.`application_status`" => "application_status",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`application_status`.`application_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`application_status`.`application_id`" => "application_id",
		"`application_status`.`application_status`" => "application_status",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`application_status`.`application_id`" => "Application Status ID:",
		"`application_status`.`application_status`" => "Application Status:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`application_status`.`application_id`" => "application_id",
		"`application_status`.`application_status`" => "application_status",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`application_status` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'application_status_view.php';
	$x->RedirectAfterInsert = 'application_status_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Application Status:';
	$x->TableIcon = 'resources/table_icons/daemons.png';
	$x->PrimaryKey = '`application_status`.`application_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Application Status ID:', 'Application Status:', ];
	$x->ColFieldName = ['application_id', 'application_status', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/application_status_templateTV.html';
	$x->SelectedTemplate = 'templates/application_status_templateTVS.html';
	$x->TemplateDV = 'templates/application_status_templateDV.html';
	$x->TemplateDVP = 'templates/application_status_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: application_status_init
	$render = true;
	if(function_exists('application_status_init')) {
		$args = [];
		$render = application_status_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: application_status_header
	$headerCode = '';
	if(function_exists('application_status_header')) {
		$args = [];
		$headerCode = application_status_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: application_status_footer
	$footerCode = '';
	if(function_exists('application_status_footer')) {
		$args = [];
		$footerCode = application_status_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
