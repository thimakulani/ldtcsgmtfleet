<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/districts.php');
	include_once(__DIR__ . '/districts_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('districts');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'districts';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`districts`.`district_id`" => "district_id",
		"`districts`.`district`" => "district",
		"`districts`.`station`" => "station",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`districts`.`district_id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`districts`.`district_id`" => "district_id",
		"`districts`.`district`" => "district",
		"`districts`.`station`" => "station",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`districts`.`district_id`" => "District ID:",
		"`districts`.`district`" => "District:",
		"`districts`.`station`" => "Government Garage/Station/Unit:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`districts`.`district_id`" => "district_id",
		"`districts`.`district`" => "district",
		"`districts`.`station`" => "station",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`districts` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'districts_view.php';
	$x->RedirectAfterInsert = 'districts_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Districts and Stations:';
	$x->TableIcon = 'resources/table_icons/Map-icon (2).png';
	$x->PrimaryKey = '`districts`.`district_id`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['District ID:', 'District:', 'Government Garage/Station/Unit:', ];
	$x->ColFieldName = ['district_id', 'district', 'station', ];
	$x->ColNumber  = [1, 2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/districts_templateTV.html';
	$x->SelectedTemplate = 'templates/districts_templateTVS.html';
	$x->TemplateDV = 'templates/districts_templateDV.html';
	$x->TemplateDVP = 'templates/districts_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: districts_init
	$render = true;
	if(function_exists('districts_init')) {
		$args = [];
		$render = districts_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: districts_header
	$headerCode = '';
	if(function_exists('districts_header')) {
		$args = [];
		$headerCode = districts_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: districts_footer
	$footerCode = '';
	if(function_exists('districts_footer')) {
		$args = [];
		$footerCode = districts_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
