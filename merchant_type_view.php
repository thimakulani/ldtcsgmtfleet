<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/merchant_type.php');
	include_once(__DIR__ . '/merchant_type_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('merchant_type');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'merchant_type';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`merchant_type`.`merchant_type_id`" => "merchant_type_id",
		"`merchant_type`.`merchant_type`" => "merchant_type",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`merchant_type`.`merchant_type_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`merchant_type`.`merchant_type_id`" => "merchant_type_id",
		"`merchant_type`.`merchant_type`" => "merchant_type",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`merchant_type`.`merchant_type_id`" => "Merchant Type ID:",
		"`merchant_type`.`merchant_type`" => "Merchant Type:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`merchant_type`.`merchant_type_id`" => "merchant_type_id",
		"`merchant_type`.`merchant_type`" => "merchant_type",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`merchant_type` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'merchant_type_view.php';
	$x->RedirectAfterInsert = 'merchant_type_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Merchant  Type:';
	$x->TableIcon = 'resources/table_icons/briefcasetype.png';
	$x->PrimaryKey = '`merchant_type`.`merchant_type_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Merchant Type ID:', 'Merchant Type:', ];
	$x->ColFieldName = ['merchant_type_id', 'merchant_type', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/merchant_type_templateTV.html';
	$x->SelectedTemplate = 'templates/merchant_type_templateTVS.html';
	$x->TemplateDV = 'templates/merchant_type_templateDV.html';
	$x->TemplateDVP = 'templates/merchant_type_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: merchant_type_init
	$render = true;
	if(function_exists('merchant_type_init')) {
		$args = [];
		$render = merchant_type_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: merchant_type_header
	$headerCode = '';
	if(function_exists('merchant_type_header')) {
		$args = [];
		$headerCode = merchant_type_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: merchant_type_footer
	$footerCode = '';
	if(function_exists('merchant_type_footer')) {
		$args = [];
		$footerCode = merchant_type_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
