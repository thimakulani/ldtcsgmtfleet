<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/dealer.php');
	include_once(__DIR__ . '/dealer_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('dealer');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'dealer';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`dealer`.`dealer_id`" => "dealer_id",
		"IF(    CHAR_LENGTH(`dealer_type1`.`dealer_type`), CONCAT_WS('',   `dealer_type1`.`dealer_type`), '') /* Dealer Type: */" => "dealer_type",
		"`dealer`.`make_of_vehicle`" => "make_of_vehicle",
		"`dealer`.`dealer_name`" => "dealer_name",
		"`dealer`.`contact_person`" => "contact_person",
		"`dealer`.`contact_details`" => "contact_details",
		"`dealer`.`contact_email`" => "contact_email",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`dealer`.`dealer_id`',
		2 => '`dealer_type1`.`dealer_type`',
		3 => 3,
		4 => 4,
		5 => 5,
		6 => 6,
		7 => 7,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`dealer`.`dealer_id`" => "dealer_id",
		"IF(    CHAR_LENGTH(`dealer_type1`.`dealer_type`), CONCAT_WS('',   `dealer_type1`.`dealer_type`), '') /* Dealer Type: */" => "dealer_type",
		"`dealer`.`make_of_vehicle`" => "make_of_vehicle",
		"`dealer`.`dealer_name`" => "dealer_name",
		"`dealer`.`contact_person`" => "contact_person",
		"`dealer`.`contact_details`" => "contact_details",
		"`dealer`.`contact_email`" => "contact_email",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`dealer`.`dealer_id`" => "Dealer ID:",
		"IF(    CHAR_LENGTH(`dealer_type1`.`dealer_type`), CONCAT_WS('',   `dealer_type1`.`dealer_type`), '') /* Dealer Type: */" => "Dealer Type:",
		"`dealer`.`make_of_vehicle`" => "Make of Vehicle:",
		"`dealer`.`dealer_name`" => "Dealer Name:",
		"`dealer`.`contact_person`" => "Contact Person:",
		"`dealer`.`contact_details`" => "Contact Details:",
		"`dealer`.`contact_email`" => "Contact E-Mail:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`dealer`.`dealer_id`" => "dealer_id",
		"IF(    CHAR_LENGTH(`dealer_type1`.`dealer_type`), CONCAT_WS('',   `dealer_type1`.`dealer_type`), '') /* Dealer Type: */" => "dealer_type",
		"`dealer`.`make_of_vehicle`" => "make_of_vehicle",
		"`dealer`.`dealer_name`" => "dealer_name",
		"`dealer`.`contact_person`" => "contact_person",
		"`dealer`.`contact_details`" => "contact_details",
		"`dealer`.`contact_email`" => "contact_email",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['dealer_type' => 'Dealer Type:', ];

	$x->QueryFrom = "`dealer` LEFT JOIN `dealer_type` as dealer_type1 ON `dealer_type1`.`dealer_type_id`=`dealer`.`dealer_type` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'dealer_view.php';
	$x->RedirectAfterInsert = 'dealer_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Dealer:';
	$x->TableIcon = 'resources/table_icons/GamingWheel.png';
	$x->PrimaryKey = '`dealer`.`dealer_id`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Dealer ID:', 'Dealer Type:', 'Make of Vehicle:', 'Dealer Name:', 'Contact Person:', 'Contact Details:', 'Contact E-Mail:', ];
	$x->ColFieldName = ['dealer_id', 'dealer_type', 'make_of_vehicle', 'dealer_name', 'contact_person', 'contact_details', 'contact_email', ];
	$x->ColNumber  = [1, 2, 3, 4, 5, 6, 7, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/dealer_templateTV.html';
	$x->SelectedTemplate = 'templates/dealer_templateTVS.html';
	$x->TemplateDV = 'templates/dealer_templateDV.html';
	$x->TemplateDVP = 'templates/dealer_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: dealer_init
	$render = true;
	if(function_exists('dealer_init')) {
		$args = [];
		$render = dealer_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: dealer_header
	$headerCode = '';
	if(function_exists('dealer_header')) {
		$args = [];
		$headerCode = dealer_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: dealer_footer
	$footerCode = '';
	if(function_exists('dealer_footer')) {
		$args = [];
		$footerCode = dealer_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
