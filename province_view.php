<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/province.php');
	include_once(__DIR__ . '/province_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('province');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'province';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`province`.`province_id`" => "province_id",
		"`province`.`province`" => "province",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`province`.`province_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`province`.`province_id`" => "province_id",
		"`province`.`province`" => "province",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`province`.`province_id`" => "Province ID:",
		"`province`.`province`" => "Province:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`province`.`province_id`" => "province_id",
		"`province`.`province`" => "province",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`province` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'province_view.php';
	$x->RedirectAfterInsert = 'province_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Province:';
	$x->TableIcon = 'resources/table_icons/limpopo.png';
	$x->PrimaryKey = '`province`.`province_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Province ID:', 'Province:', ];
	$x->ColFieldName = ['province_id', 'province', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/province_templateTV.html';
	$x->SelectedTemplate = 'templates/province_templateTVS.html';
	$x->TemplateDV = 'templates/province_templateDV.html';
	$x->TemplateDVP = 'templates/province_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: province_init
	$render = true;
	if(function_exists('province_init')) {
		$args = [];
		$render = province_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: province_header
	$headerCode = '';
	if(function_exists('province_header')) {
		$args = [];
		$headerCode = province_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: province_footer
	$footerCode = '';
	if(function_exists('province_footer')) {
		$args = [];
		$footerCode = province_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
