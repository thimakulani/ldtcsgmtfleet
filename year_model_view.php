<?php
// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/year_model.php');
	include_once(__DIR__ . '/year_model_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('year_model');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'year_model';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`year_model`.`year_model_id`" => "year_model_id",
		"`year_model`.`year_model_specification`" => "year_model_specification",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`year_model`.`year_model_id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`year_model`.`year_model_id`" => "year_model_id",
		"`year_model`.`year_model_specification`" => "year_model_specification",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`year_model`.`year_model_id`" => "Year Model ID:",
		"`year_model`.`year_model_specification`" => "Year Model Specification:",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`year_model`.`year_model_id`" => "year_model_id",
		"`year_model`.`year_model_specification`" => "year_model_specification",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`year_model` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 25;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'year_model_view.php';
	$x->RedirectAfterInsert = 'year_model_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Year Model:';
	$x->TableIcon = 'resources/table_icons/Numbers-icon.png';
	$x->PrimaryKey = '`year_model`.`year_model_id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Year Model ID:', 'Year Model Specification:', ];
	$x->ColFieldName = ['year_model_id', 'year_model_specification', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/year_model_templateTV.html';
	$x->SelectedTemplate = 'templates/year_model_templateTVS.html';
	$x->TemplateDV = 'templates/year_model_templateDV.html';
	$x->TemplateDVP = 'templates/year_model_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: year_model_init
	$render = true;
	if(function_exists('year_model_init')) {
		$args = [];
		$render = year_model_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: year_model_header
	$headerCode = '';
	if(function_exists('year_model_header')) {
		$args = [];
		$headerCode = year_model_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: year_model_footer
	$footerCode = '';
	if(function_exists('year_model_footer')) {
		$args = [];
		$footerCode = year_model_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
