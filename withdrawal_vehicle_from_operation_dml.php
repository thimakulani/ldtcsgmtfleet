<?php

// Data functions (insert, update, delete, form) for table withdrawal_vehicle_from_operation

// This script and data application were generated by AppGini 22.13
// Download AppGini for free from https://bigprof.com/appgini/download/

function withdrawal_vehicle_from_operation_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('withdrawal_vehicle_from_operation');
	if(!$arrPerm['insert']) return false;

	$data = [
		'supervisor_name_and_surname' => Request::val('supervisor_name_and_surname', ''),
		'supervisor_contact_details' => Request::val('supervisor_contact_details', ''),
		'supervisor_email_address' => Request::val('supervisor_email_address', ''),
		'supervisor_signature' => Request::fileUpload('supervisor_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('withdrawal_vehicle_from_operation', 'supervisor_signature', Request::val('SelectedID'));
			},
		]),
		'vehicle_registration_number' => Request::lookup('vehicle_registration_number', ''),
		'engine_number' => Request::lookup('vehicle_registration_number'),
		'make_of_vehicle' => Request::lookup('vehicle_registration_number'),
		'purchased_price' => Request::lookup('vehicle_registration_number'),
		'date_of_service' => Request::lookup('date_of_service', ''),
		'date_of_next_service' => Request::lookup('date_of_service'),
		'renewal_of_license' => Request::lookup('vehicle_registration_number'),
		'date_of_vehicle' => mysql_datetime(Request::val('date_of_vehicle', '')),
		'description_of_vehicle_breakdown' => Request::val('description_of_vehicle_breakdown', ''),
		'tyre_inspection_report' => Request::fileUpload('tyre_inspection_report', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('withdrawal_vehicle_from_operation', 'tyre_inspection_report', Request::val('SelectedID'));
			},
		]),
		'document_checklist_report' => Request::fileUpload('document_checklist_report', [
			'maxSize' => 5365760,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('withdrawal_vehicle_from_operation', 'document_checklist_report', Request::val('SelectedID'));
			},
		]),
		'compiled_technical_report' => Request::fileUpload('compiled_technical_report', [
			'maxSize' => 5365760,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => true,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('withdrawal_vehicle_from_operation', 'compiled_technical_report', Request::val('SelectedID'));
			},
		]),
		'district_officer_name_and_surname' => Request::val('district_officer_name_and_surname', ''),
		'district_officer_persal_number' => Request::val('district_officer_persal_number', ''),
		'district_officer_contacts' => Request::val('district_officer_contacts', ''),
		'district_officer_signature' => Request::fileUpload('district_officer_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'success' => function($name, $selected_id) {
			},
			'failure' => function($selected_id, $fileRemoved) {
				if(!strlen(Request::val('SelectedID'))) return '';

				/* for empty upload fields, when saving a copy of an existing record, copy the original upload field */
				return existing_value('withdrawal_vehicle_from_operation', 'district_officer_signature', Request::val('SelectedID'));
			},
		]),
		'district_officer_email_address' => Request::val('district_officer_email_address', ''),
	];


	// hook: withdrawal_vehicle_from_operation_before_insert
	if(function_exists('withdrawal_vehicle_from_operation_before_insert')) {
		$args = [];
		if(!withdrawal_vehicle_from_operation_before_insert($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$error = '';
	// set empty fields to NULL
	$data = array_map(function($v) { return ($v === '' ? NULL : $v); }, $data);
	insert('withdrawal_vehicle_from_operation', backtick_keys_once($data), $error);
	if($error) {
		$error_message = $error;
		return false;
	}

	$recID = db_insert_id(db_link());

	update_calc_fields('withdrawal_vehicle_from_operation', $recID, calculated_fields()['withdrawal_vehicle_from_operation']);

	// hook: withdrawal_vehicle_from_operation_after_insert
	if(function_exists('withdrawal_vehicle_from_operation_after_insert')) {
		$res = sql("SELECT * FROM `withdrawal_vehicle_from_operation` WHERE `withdrawal_id`='" . makeSafe($recID, false) . "' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) {
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = makeSafe($recID, false);
		$args = [];
		if(!withdrawal_vehicle_from_operation_after_insert($data, getMemberInfo(), $args)) { return $recID; }
	}

	// mm: save ownership data
	set_record_owner('withdrawal_vehicle_from_operation', $recID, getLoggedMemberID());

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID'))) withdrawal_vehicle_from_operation_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function withdrawal_vehicle_from_operation_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);

	// launch requests, asynchronously
	curl_batch($requests);
}

function withdrawal_vehicle_from_operation_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('withdrawal_vehicle_from_operation', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: withdrawal_vehicle_from_operation_before_delete
	if(function_exists('withdrawal_vehicle_from_operation_before_delete')) {
		$args = [];
		if(!withdrawal_vehicle_from_operation_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: '' 
			);
	}

	sql("DELETE FROM `withdrawal_vehicle_from_operation` WHERE `withdrawal_id`='{$selected_id}'", $eo);

	// hook: withdrawal_vehicle_from_operation_after_delete
	if(function_exists('withdrawal_vehicle_from_operation_after_delete')) {
		$args = [];
		withdrawal_vehicle_from_operation_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='withdrawal_vehicle_from_operation' AND `pkValue`='{$selected_id}'", $eo);
}

function withdrawal_vehicle_from_operation_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('withdrawal_vehicle_from_operation', $selected_id, 'edit')) return false;

	$data = [
		'supervisor_name_and_surname' => Request::val('supervisor_name_and_surname', ''),
		'supervisor_contact_details' => Request::val('supervisor_contact_details', ''),
		'supervisor_email_address' => Request::val('supervisor_email_address', ''),
		'supervisor_signature' => Request::fileUpload('supervisor_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('withdrawal_vehicle_from_operation', 'supervisor_signature', $selected_id);
			},
		]),
		'vehicle_registration_number' => Request::lookup('vehicle_registration_number', ''),
		'engine_number' => Request::lookup('vehicle_registration_number'),
		'make_of_vehicle' => Request::lookup('vehicle_registration_number'),
		'purchased_price' => Request::lookup('vehicle_registration_number'),
		'date_of_service' => Request::lookup('date_of_service', ''),
		'date_of_next_service' => Request::lookup('date_of_service'),
		'renewal_of_license' => Request::lookup('vehicle_registration_number'),
		'date_of_vehicle' => mysql_datetime(Request::val('date_of_vehicle', '')),
		'description_of_vehicle_breakdown' => Request::val('description_of_vehicle_breakdown', ''),
		'tyre_inspection_report' => Request::fileUpload('tyre_inspection_report', [
			'maxSize' => 5365760,
			'types' => 'txt|doc|docx|docm|odt|pdf|rtf',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('withdrawal_vehicle_from_operation', 'tyre_inspection_report', $selected_id);
			},
		]),
		'document_checklist_report' => Request::fileUpload('document_checklist_report', [
			'maxSize' => 5365760,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('withdrawal_vehicle_from_operation', 'document_checklist_report', $selected_id);
			},
		]),
		'compiled_technical_report' => Request::fileUpload('compiled_technical_report', [
			'maxSize' => 5365760,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => true,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('withdrawal_vehicle_from_operation', 'compiled_technical_report', $selected_id);
			},
		]),
		'district_officer_name_and_surname' => Request::val('district_officer_name_and_surname', ''),
		'district_officer_persal_number' => Request::val('district_officer_persal_number', ''),
		'district_officer_contacts' => Request::val('district_officer_contacts', ''),
		'district_officer_signature' => Request::fileUpload('district_officer_signature', [
			'maxSize' => 512000,
			'types' => 'jpg|jpeg|gif|png|webp',
			'noRename' => false,
			'dir' => '',
			'id' => $selected_id,
			'success' => function($name, $selected_id) {
			},
			'removeOnRequest' => true,
			'remove' => function($selected_id) {
				// do nothing: preserve removed files on server.
			},
			'failure' => function($selected_id, $fileRemoved) {
				if($fileRemoved) return '';
				return existing_value('withdrawal_vehicle_from_operation', 'district_officer_signature', $selected_id);
			},
		]),
		'district_officer_email_address' => Request::val('district_officer_email_address', ''),
	];

	// get existing values
	$old_data = getRecord('withdrawal_vehicle_from_operation', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: withdrawal_vehicle_from_operation_before_update
	if(function_exists('withdrawal_vehicle_from_operation_before_update')) {
		$args = ['old_data' => $old_data];
		if(!withdrawal_vehicle_from_operation_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'withdrawal_vehicle_from_operation', 
		backtick_keys_once($set), 
		['`withdrawal_id`' => $selected_id], 
		$error_message
	)) {
		echo $error_message;
		echo '<a href="withdrawal_vehicle_from_operation_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	$eo = ['silentErrors' => true];

	update_calc_fields('withdrawal_vehicle_from_operation', $data['selectedID'], calculated_fields()['withdrawal_vehicle_from_operation']);

	// hook: withdrawal_vehicle_from_operation_after_update
	if(function_exists('withdrawal_vehicle_from_operation_after_update')) {
		$res = sql("SELECT * FROM `withdrawal_vehicle_from_operation` WHERE `withdrawal_id`='{$data['selectedID']}' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['withdrawal_id'];
		$args = ['old_data' => $old_data];
		if(!withdrawal_vehicle_from_operation_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update ownership data
	sql("UPDATE `membership_userrecords` SET `dateUpdated`='" . time() . "' WHERE `tableName`='withdrawal_vehicle_from_operation' AND `pkValue`='" . makeSafe($selected_id) . "'", $eo);
}

function withdrawal_vehicle_from_operation_form($selected_id = '', $AllowUpdate = 1, $AllowInsert = 1, $AllowDelete = 1, $separateDV = 0, $TemplateDV = '', $TemplateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selected_id. If $selected_id
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = null;
	$row = $urow = $jsReadOnly = $jsEditable = $lookups = null;

	$noSaveAsCopy = true;

	// mm: get table permissions
	$arrPerm = getTablePermissions('withdrawal_vehicle_from_operation');
	if(!$arrPerm['insert'] && $selected_id == '')
		// no insert permission and no record selected
		// so show access denied error unless TVDV
		return $separateDV ? $Translation['tableAccessDenied'] : '';
	$AllowInsert = ($arrPerm['insert'] ? true : false);
	// print preview?
	$dvprint = false;
	if(strlen($selected_id) && Request::val('dvprint_x') != '') {
		$dvprint = true;
	}

	$filterer_vehicle_registration_number = Request::val('filterer_vehicle_registration_number');
	$filterer_date_of_service = Request::val('filterer_date_of_service');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: vehicle_registration_number
	$combo_vehicle_registration_number = new DataCombo;
	// combobox: date_of_service
	$combo_date_of_service = new DataCombo;

	if($selected_id) {
		// mm: check member permissions
		if(!$arrPerm['view']) return $Translation['tableAccessDenied'];

		// mm: who is the owner?
		$ownerGroupID = sqlValue("SELECT `groupID` FROM `membership_userrecords` WHERE `tableName`='withdrawal_vehicle_from_operation' AND `pkValue`='" . makeSafe($selected_id) . "'");
		$ownerMemberID = sqlValue("SELECT LCASE(`memberID`) FROM `membership_userrecords` WHERE `tableName`='withdrawal_vehicle_from_operation' AND `pkValue`='" . makeSafe($selected_id) . "'");

		if($arrPerm['view'] == 1 && getLoggedMemberID() != $ownerMemberID) return $Translation['tableAccessDenied'];
		if($arrPerm['view'] == 2 && getLoggedGroupID() != $ownerGroupID) return $Translation['tableAccessDenied'];

		// can edit?
		$AllowUpdate = 0;
		if(($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3) {
			$AllowUpdate = 1;
		}

		$res = sql("SELECT * FROM `withdrawal_vehicle_from_operation` WHERE `withdrawal_id`='" . makeSafe($selected_id) . "'", $eo);
		if(!($row = db_fetch_array($res))) {
			return error_message($Translation['No records found'], 'withdrawal_vehicle_from_operation_view.php', false);
		}
		$combo_vehicle_registration_number->SelectedData = $row['vehicle_registration_number'];
		$combo_date_of_service->SelectedData = $row['date_of_service'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_vehicle_registration_number->SelectedData = $filterer_vehicle_registration_number;
		$combo_date_of_service->SelectedData = $filterer_date_of_service;
	}
	$combo_vehicle_registration_number->HTML = '<span id="vehicle_registration_number-container' . $rnd1 . '"></span><input type="hidden" name="vehicle_registration_number" id="vehicle_registration_number' . $rnd1 . '" value="' . html_attr($combo_vehicle_registration_number->SelectedData) . '">';
	$combo_vehicle_registration_number->MatchText = '<span id="vehicle_registration_number-container-readonly' . $rnd1 . '"></span><input type="hidden" name="vehicle_registration_number" id="vehicle_registration_number' . $rnd1 . '" value="' . html_attr($combo_vehicle_registration_number->SelectedData) . '">';
	$combo_date_of_service->HTML = '<span id="date_of_service-container' . $rnd1 . '"></span><input type="hidden" name="date_of_service" id="date_of_service' . $rnd1 . '" value="' . html_attr($combo_date_of_service->SelectedData) . '">';
	$combo_date_of_service->MatchText = '<span id="date_of_service-container-readonly' . $rnd1 . '"></span><input type="hidden" name="date_of_service" id="date_of_service' . $rnd1 . '" value="' . html_attr($combo_date_of_service->SelectedData) . '">';

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_vehicle_registration_number__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['vehicle_registration_number'] : htmlspecialchars($filterer_vehicle_registration_number, ENT_QUOTES)); ?>"};
		AppGini.current_date_of_service__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['date_of_service'] : htmlspecialchars($filterer_date_of_service, ENT_QUOTES)); ?>"};

		jQuery(function() {
			setTimeout(function() {
				if(typeof(vehicle_registration_number_reload__RAND__) == 'function') vehicle_registration_number_reload__RAND__();
				if(typeof(date_of_service_reload__RAND__) == 'function') date_of_service_reload__RAND__();
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function vehicle_registration_number_reload__RAND__() {
		<?php if(($AllowUpdate || ($arrPerm['insert'] && !$selected_id)) && !$dvprint) { ?>

			$j("#vehicle_registration_number-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_vehicle_registration_number__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'vehicle_registration_number' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="vehicle_registration_number"]').val(resp.results[0].id);
							$j('[id=vehicle_registration_number-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_registration_number-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=gmt_fleet_register_view_parent]').hide(); } else { $j('.btn[id=gmt_fleet_register_view_parent]').show(); }


							if(typeof(vehicle_registration_number_update_autofills__RAND__) == 'function') vehicle_registration_number_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'withdrawal_vehicle_from_operation', f: 'vehicle_registration_number' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_vehicle_registration_number__RAND__.value = e.added.id;
				AppGini.current_vehicle_registration_number__RAND__.text = e.added.text;
				$j('[name="vehicle_registration_number"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=gmt_fleet_register_view_parent]').hide(); } else { $j('.btn[id=gmt_fleet_register_view_parent]').show(); }


				if(typeof(vehicle_registration_number_update_autofills__RAND__) == 'function') vehicle_registration_number_update_autofills__RAND__();
			});

			if(!$j("#vehicle_registration_number-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_vehicle_registration_number__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'vehicle_registration_number' },
					success: function(resp) {
						$j('[name="vehicle_registration_number"]').val(resp.results[0].id);
						$j('[id=vehicle_registration_number-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_registration_number-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=gmt_fleet_register_view_parent]').hide(); } else { $j('.btn[id=gmt_fleet_register_view_parent]').show(); }

						if(typeof(vehicle_registration_number_update_autofills__RAND__) == 'function') vehicle_registration_number_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_vehicle_registration_number__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'vehicle_registration_number' },
				success: function(resp) {
					$j('[id=vehicle_registration_number-container__RAND__], [id=vehicle_registration_number-container-readonly__RAND__]').html('<span class="match-text" id="vehicle_registration_number-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=gmt_fleet_register_view_parent]').hide(); } else { $j('.btn[id=gmt_fleet_register_view_parent]').show(); }

					if(typeof(vehicle_registration_number_update_autofills__RAND__) == 'function') vehicle_registration_number_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
		function date_of_service_reload__RAND__() {
		<?php if(($AllowUpdate || ($arrPerm['insert'] && !$selected_id)) && !$dvprint) { ?>

			$j("#date_of_service-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_date_of_service__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'date_of_service' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="date_of_service"]').val(resp.results[0].id);
							$j('[id=date_of_service-container-readonly__RAND__]').html('<span class="match-text" id="date_of_service-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=service_view_parent]').hide(); } else { $j('.btn[id=service_view_parent]').show(); }


							if(typeof(date_of_service_update_autofills__RAND__) == 'function') date_of_service_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'withdrawal_vehicle_from_operation', f: 'date_of_service' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_date_of_service__RAND__.value = e.added.id;
				AppGini.current_date_of_service__RAND__.text = e.added.text;
				$j('[name="date_of_service"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=service_view_parent]').hide(); } else { $j('.btn[id=service_view_parent]').show(); }


				if(typeof(date_of_service_update_autofills__RAND__) == 'function') date_of_service_update_autofills__RAND__();
			});

			if(!$j("#date_of_service-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_date_of_service__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'date_of_service' },
					success: function(resp) {
						$j('[name="date_of_service"]').val(resp.results[0].id);
						$j('[id=date_of_service-container-readonly__RAND__]').html('<span class="match-text" id="date_of_service-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=service_view_parent]').hide(); } else { $j('.btn[id=service_view_parent]').show(); }

						if(typeof(date_of_service_update_autofills__RAND__) == 'function') date_of_service_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_date_of_service__RAND__.value, t: 'withdrawal_vehicle_from_operation', f: 'date_of_service' },
				success: function(resp) {
					$j('[id=date_of_service-container__RAND__], [id=date_of_service-container-readonly__RAND__]').html('<span class="match-text" id="date_of_service-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=service_view_parent]').hide(); } else { $j('.btn[id=service_view_parent]').show(); }

					if(typeof(date_of_service_update_autofills__RAND__) == 'function') date_of_service_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$TemplateDVP}") ? "./{$TemplateDVP}" : './templates/withdrawal_vehicle_from_operation_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$TemplateDV}") ? "./{$TemplateDV}" : './templates/withdrawal_vehicle_from_operation_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Withdrawal of Vehicle From Operation ID Details:', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($arrPerm['insert'] && !$selected_id) { // allow insert and no record selected?
		if(!$selected_id) $templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1" onclick="return withdrawal_vehicle_from_operation_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1" onclick="return withdrawal_vehicle_from_operation_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = '$j(\'form\').eq(0).attr(\'novalidate\', \'novalidate\'); document.myform.reset(); return true;';
	}

	if($selected_id) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" onclick="$j(\'form\').eq(0).prop(\'novalidate\', true); document.myform.reset(); return true;" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($AllowUpdate) {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" onclick="return withdrawal_vehicle_from_operation_validateData();" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		}
		if(($arrPerm['delete'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['delete'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['delete'] == 3) { // allow delete?
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" onclick="return confirm(\'' . $Translation['are you sure?'] . '\');" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		}
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$arrPerm['insert']
			&& !$arrPerm['update'] && !$arrPerm['delete'] && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>', 
				'<button
					type="submit" 
					class="btn btn-default" 
					id="deselect" 
					name="deselect_x" 
					value="1" 
					onclick="' . $backAction . '" 
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(($selected_id && !$AllowUpdate) || (!$selected_id && !$AllowInsert)) {
		$jsReadOnly = '';
		$jsReadOnly .= "\tjQuery('#supervisor_name_and_surname').replaceWith('<div class=\"form-control-static\" id=\"supervisor_name_and_surname\">' + (jQuery('#supervisor_name_and_surname').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#supervisor_contact_details').replaceWith('<div class=\"form-control-static\" id=\"supervisor_contact_details\">' + (jQuery('#supervisor_contact_details').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#supervisor_email_address').replaceWith('<div class=\"form-control-static\" id=\"supervisor_email_address\">' + (jQuery('#supervisor_email_address').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#supervisor_email_address, #supervisor_email_address-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#supervisor_signature').replaceWith('<div class=\"form-control-static\" id=\"supervisor_signature\">' + (jQuery('#supervisor_signature').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#vehicle_registration_number').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#vehicle_registration_number_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#date_of_service').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#date_of_service_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#date_of_vehicle').parents('.input-group').replaceWith('<div class=\"form-control-static\" id=\"date_of_vehicle\">' + (jQuery('#date_of_vehicle').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#tyre_inspection_report').replaceWith('<div class=\"form-control-static\" id=\"tyre_inspection_report\">' + (jQuery('#tyre_inspection_report').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#tyre_inspection_report, #tyre_inspection_report-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#document_checklist_report').replaceWith('<div class=\"form-control-static\" id=\"document_checklist_report\">' + (jQuery('#document_checklist_report').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#document_checklist_report, #document_checklist_report-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#compiled_technical_report').replaceWith('<div class=\"form-control-static\" id=\"compiled_technical_report\">' + (jQuery('#compiled_technical_report').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#compiled_technical_report, #compiled_technical_report-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('#district_officer_name_and_surname').replaceWith('<div class=\"form-control-static\" id=\"district_officer_name_and_surname\">' + (jQuery('#district_officer_name_and_surname').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#district_officer_persal_number').replaceWith('<div class=\"form-control-static\" id=\"district_officer_persal_number\">' + (jQuery('#district_officer_persal_number').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#district_officer_contacts').replaceWith('<div class=\"form-control-static\" id=\"district_officer_contacts\">' + (jQuery('#district_officer_contacts').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#district_officer_signature').replaceWith('<div class=\"form-control-static\" id=\"district_officer_signature\">' + (jQuery('#district_officer_signature').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#district_officer_email_address').replaceWith('<div class=\"form-control-static\" id=\"district_officer_email_address\">' + (jQuery('#district_officer_email_address').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#district_officer_email_address, #district_officer_email_address-edit-link').hide();\n";
		$jsReadOnly .= "\tjQuery('.select2-container').hide();\n";

		$noUploads = true;
	} elseif(($AllowInsert && !$selected_id) || ($AllowUpdate && $selected_id)) {
		$jsEditable = "\tjQuery('form').eq(0).data('already_changed', true);"; // temporarily disable form change handler
		$locale = isset($Translation['datetimepicker locale']) ? ", locale: '{$Translation['datetimepicker locale']}'" : '';
		$jsEditable .= "\tjQuery('#date_of_vehicle').addClass('always_shown').parents('.input-group').datetimepicker({ toolbarPlacement: 'top', sideBySide: true, showClear: true, showTodayButton: true, showClose: true, icons: { close: 'glyphicon glyphicon-ok' }, format: AppGini.datetimeFormat('dt') {$locale} });";
		$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(vehicle_registration_number)%%>', $combo_vehicle_registration_number->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(vehicle_registration_number)%%>', $combo_vehicle_registration_number->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(vehicle_registration_number)%%>', urlencode($combo_vehicle_registration_number->MatchText), $templateCode);
	$templateCode = str_replace('<%%COMBO(date_of_service)%%>', $combo_date_of_service->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(date_of_service)%%>', $combo_date_of_service->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(date_of_service)%%>', urlencode($combo_date_of_service->MatchText), $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = ['vehicle_registration_number' => ['gmt_fleet_register', 'Vehicle Registration Number:'], 'date_of_service' => ['service', 'Date Of Service:'], ];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if($pt_perm['view'] || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(withdrawal_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(supervisor_name_and_surname)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(supervisor_contact_details)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(supervisor_email_address)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(supervisor_signature)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="supervisor_signature" id="supervisor_signature" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="512000" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(vehicle_registration_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(date_of_service)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(date_of_vehicle)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(description_of_vehicle_breakdown)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(tyre_inspection_report)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="tyre_inspection_report" id="tyre_inspection_report" data-filetypes="txt|doc|docx|docm|odt|pdf|rtf" data-maxsize="5365760" accept=".txt,.doc,.docx,.docm,.odt,.pdf,.rtf">'), $templateCode);
	if($AllowUpdate && $row['tyre_inspection_report'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(tyre_inspection_report)%%>', '<br><input type="checkbox" name="tyre_inspection_report_remove" id="tyre_inspection_report_remove" value="1"> <label for="tyre_inspection_report_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(tyre_inspection_report)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(document_checklist_report)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="document_checklist_report" id="document_checklist_report" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="5365760" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	if($AllowUpdate && $row['document_checklist_report'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(document_checklist_report)%%>', '<br><input type="checkbox" name="document_checklist_report_remove" id="document_checklist_report_remove" value="1"> <label for="document_checklist_report_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(document_checklist_report)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(compiled_technical_report)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="compiled_technical_report" id="compiled_technical_report" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="5365760" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	if($AllowUpdate && $row['compiled_technical_report'] != '') {
		$templateCode = str_replace('<%%REMOVEFILE(compiled_technical_report)%%>', '<br><input type="checkbox" name="compiled_technical_report_remove" id="compiled_technical_report_remove" value="1"> <label for="compiled_technical_report_remove" style="color: red; font-weight: bold;">'.$Translation['remove image'].'</label>', $templateCode);
	} else {
		$templateCode = str_replace('<%%REMOVEFILE(compiled_technical_report)%%>', '', $templateCode);
	}
	$templateCode = str_replace('<%%UPLOADFILE(district_officer_name_and_surname)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(district_officer_persal_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(district_officer_contacts)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(district_officer_signature)%%>', ($noUploads ? '' : "<div>{$Translation['upload image']}</div>" . '<i class="glyphicon glyphicon-remove text-danger clear-upload hidden"></i> <input type="file" name="district_officer_signature" id="district_officer_signature" data-filetypes="jpg|jpeg|gif|png|webp" data-maxsize="512000" accept=".jpg,.jpeg,.gif,.png,.webp">'), $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(district_officer_email_address)%%>', '', $templateCode);

	// process values
	if($selected_id) {
		if( $dvprint) $templateCode = str_replace('<%%VALUE(withdrawal_id)%%>', safe_html($urow['withdrawal_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(withdrawal_id)%%>', html_attr($row['withdrawal_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(withdrawal_id)%%>', urlencode($urow['withdrawal_id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(supervisor_name_and_surname)%%>', safe_html($urow['supervisor_name_and_surname']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(supervisor_name_and_surname)%%>', html_attr($row['supervisor_name_and_surname']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_name_and_surname)%%>', urlencode($urow['supervisor_name_and_surname']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(supervisor_contact_details)%%>', safe_html($urow['supervisor_contact_details']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(supervisor_contact_details)%%>', html_attr($row['supervisor_contact_details']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_contact_details)%%>', urlencode($urow['supervisor_contact_details']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(supervisor_email_address)%%>', safe_html($urow['supervisor_email_address']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(supervisor_email_address)%%>', html_attr($row['supervisor_email_address']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_email_address)%%>', urlencode($urow['supervisor_email_address']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(supervisor_signature)%%>', safe_html($urow['supervisor_signature']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(supervisor_signature)%%>', html_attr($row['supervisor_signature']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_signature)%%>', urlencode($urow['supervisor_signature']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', safe_html($urow['vehicle_registration_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', html_attr($row['vehicle_registration_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_registration_number)%%>', urlencode($urow['vehicle_registration_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(date_of_service)%%>', safe_html($urow['date_of_service']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(date_of_service)%%>', html_attr($row['date_of_service']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(date_of_service)%%>', urlencode($urow['date_of_service']), $templateCode);
		$templateCode = str_replace('<%%VALUE(date_of_vehicle)%%>', app_datetime($row['date_of_vehicle'], 'dt'), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(date_of_vehicle)%%>', urlencode(app_datetime($urow['date_of_vehicle'], 'dt')), $templateCode);
		if($AllowUpdate || $AllowInsert) {
			$templateCode = str_replace('<%%HTMLAREA(description_of_vehicle_breakdown)%%>', '<textarea name="description_of_vehicle_breakdown" id="description_of_vehicle_breakdown" rows="5">' . safe_html(htmlspecialchars_decode($row['description_of_vehicle_breakdown'])) . '</textarea>', $templateCode);
		} else {
			$templateCode = str_replace('<%%HTMLAREA(description_of_vehicle_breakdown)%%>', '<div id="description_of_vehicle_breakdown" class="form-control-static">' . $row['description_of_vehicle_breakdown'] . '</div>', $templateCode);
		}
		$templateCode = str_replace('<%%VALUE(description_of_vehicle_breakdown)%%>', nl2br($row['description_of_vehicle_breakdown']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(description_of_vehicle_breakdown)%%>', urlencode($urow['description_of_vehicle_breakdown']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(tyre_inspection_report)%%>', safe_html($urow['tyre_inspection_report']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(tyre_inspection_report)%%>', html_attr($row['tyre_inspection_report']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(tyre_inspection_report)%%>', urlencode($urow['tyre_inspection_report']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(document_checklist_report)%%>', safe_html($urow['document_checklist_report']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(document_checklist_report)%%>', html_attr($row['document_checklist_report']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(document_checklist_report)%%>', urlencode($urow['document_checklist_report']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(compiled_technical_report)%%>', safe_html($urow['compiled_technical_report']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(compiled_technical_report)%%>', html_attr($row['compiled_technical_report']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(compiled_technical_report)%%>', urlencode($urow['compiled_technical_report']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(district_officer_name_and_surname)%%>', safe_html($urow['district_officer_name_and_surname']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(district_officer_name_and_surname)%%>', html_attr($row['district_officer_name_and_surname']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_name_and_surname)%%>', urlencode($urow['district_officer_name_and_surname']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(district_officer_persal_number)%%>', safe_html($urow['district_officer_persal_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(district_officer_persal_number)%%>', html_attr($row['district_officer_persal_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_persal_number)%%>', urlencode($urow['district_officer_persal_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(district_officer_contacts)%%>', safe_html($urow['district_officer_contacts']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(district_officer_contacts)%%>', html_attr($row['district_officer_contacts']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_contacts)%%>', urlencode($urow['district_officer_contacts']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(district_officer_signature)%%>', safe_html($urow['district_officer_signature']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(district_officer_signature)%%>', html_attr($row['district_officer_signature']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_signature)%%>', urlencode($urow['district_officer_signature']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(district_officer_email_address)%%>', safe_html($urow['district_officer_email_address']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(district_officer_email_address)%%>', html_attr($row['district_officer_email_address']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_email_address)%%>', urlencode($urow['district_officer_email_address']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(withdrawal_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(withdrawal_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(supervisor_name_and_surname)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_name_and_surname)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(supervisor_contact_details)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_contact_details)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(supervisor_email_address)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_email_address)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(supervisor_signature)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(supervisor_signature)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(vehicle_registration_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(vehicle_registration_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(date_of_service)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(date_of_service)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(date_of_vehicle)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(date_of_vehicle)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%HTMLAREA(description_of_vehicle_breakdown)%%>', '<textarea name="description_of_vehicle_breakdown" id="description_of_vehicle_breakdown" rows="5"></textarea>', $templateCode);
		$templateCode = str_replace('<%%VALUE(tyre_inspection_report)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(tyre_inspection_report)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(document_checklist_report)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(document_checklist_report)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(compiled_technical_report)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(compiled_technical_report)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(district_officer_name_and_surname)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_name_and_surname)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(district_officer_persal_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_persal_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(district_officer_contacts)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_contacts)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(district_officer_signature)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_signature)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(district_officer_email_address)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(district_officer_email_address)%%>', urlencode(''), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$selected_id) {
			$templateCode.="\n\tif(document.getElementById('supervisor_email_addressEdit')) { document.getElementById('supervisor_email_addressEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('supervisor_email_addressEditLink')) { document.getElementById('supervisor_email_addressEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('tyre_inspection_reportEdit')) { document.getElementById('tyre_inspection_reportEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('tyre_inspection_reportEditLink')) { document.getElementById('tyre_inspection_reportEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('document_checklist_reportEdit')) { document.getElementById('document_checklist_reportEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('document_checklist_reportEditLink')) { document.getElementById('document_checklist_reportEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('compiled_technical_reportEdit')) { document.getElementById('compiled_technical_reportEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('compiled_technical_reportEditLink')) { document.getElementById('compiled_technical_reportEditLink').style.display='none'; }";
			$templateCode.="\n\tif(document.getElementById('district_officer_email_addressEdit')) { document.getElementById('district_officer_email_addressEdit').style.display='inline'; }";
			$templateCode.="\n\tif(document.getElementById('district_officer_email_addressEditLink')) { document.getElementById('district_officer_email_addressEditLink').style.display='none'; }";
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';

	$templateCode .= "\tvehicle_registration_number_update_autofills$rnd1 = function() {\n";
	$templateCode .= "\t\t\$j.ajax({\n";
	if($dvprint) {
		$templateCode .= "\t\t\turl: 'withdrawal_vehicle_from_operation_autofill.php?rnd1=$rnd1&mfk=vehicle_registration_number&id=' + encodeURIComponent('".addslashes($row['vehicle_registration_number'])."'),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET'\n";
	} else {
		$templateCode .= "\t\t\turl: 'withdrawal_vehicle_from_operation_autofill.php?rnd1=$rnd1&mfk=vehicle_registration_number&id=' + encodeURIComponent(AppGini.current_vehicle_registration_number{$rnd1}.value),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET',\n";
		$templateCode .= "\t\t\tbeforeSend: function() { \$j('#vehicle_registration_number$rnd1').prop('disabled', true); },\n";
		$templateCode .= "\t\t\tcomplete: function() { " . (($arrPerm['insert'] || (($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3)) ? "\$j('#vehicle_registration_number$rnd1').prop('disabled', false); " : "\$j('#vehicle_registration_number$rnd1').prop('disabled', true); ")." \$j(window).resize(); }\n";
	}
	$templateCode .= "\t\t});\n";
	$templateCode .= "\t};\n";
	if(!$dvprint) $templateCode .= "\tif(\$j('#vehicle_registration_number_caption').length) \$j('#vehicle_registration_number_caption').click(function() { vehicle_registration_number_update_autofills$rnd1(); });\n";

	$templateCode .= "\tdate_of_service_update_autofills$rnd1 = function() {\n";
	$templateCode .= "\t\t\$j.ajax({\n";
	if($dvprint) {
		$templateCode .= "\t\t\turl: 'withdrawal_vehicle_from_operation_autofill.php?rnd1=$rnd1&mfk=date_of_service&id=' + encodeURIComponent('".addslashes($row['date_of_service'])."'),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET'\n";
	} else {
		$templateCode .= "\t\t\turl: 'withdrawal_vehicle_from_operation_autofill.php?rnd1=$rnd1&mfk=date_of_service&id=' + encodeURIComponent(AppGini.current_date_of_service{$rnd1}.value),\n";
		$templateCode .= "\t\t\tcontentType: 'application/x-www-form-urlencoded; charset=" . datalist_db_encoding . "',\n";
		$templateCode .= "\t\t\ttype: 'GET',\n";
		$templateCode .= "\t\t\tbeforeSend: function() { \$j('#date_of_service$rnd1').prop('disabled', true); },\n";
		$templateCode .= "\t\t\tcomplete: function() { " . (($arrPerm['insert'] || (($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3)) ? "\$j('#date_of_service$rnd1').prop('disabled', false); " : "\$j('#date_of_service$rnd1').prop('disabled', true); ")." \$j(window).resize(); }\n";
	}
	$templateCode .= "\t\t});\n";
	$templateCode .= "\t};\n";
	if(!$dvprint) $templateCode .= "\tif(\$j('#date_of_service_caption').length) \$j('#date_of_service_caption').click(function() { date_of_service_update_autofills$rnd1(); });\n";


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('withdrawal_vehicle_from_operation');
	if($selected_id) {
		$jdata = get_joined_record('withdrawal_vehicle_from_operation', $selected_id);
		if($jdata === false) $jdata = get_defaults('withdrawal_vehicle_from_operation');
		$rdata = $row;
	}
	$templateCode .= loadView('withdrawal_vehicle_from_operation-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: withdrawal_vehicle_from_operation_dv
	if(function_exists('withdrawal_vehicle_from_operation_dv')) {
		$args = [];
		withdrawal_vehicle_from_operation_dv(($selected_id ? $selected_id : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}